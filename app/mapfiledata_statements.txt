    DATA "the_geom from (select bgdi_id, p_gesamt, ffpot_d, ffpot_e, %geometry% as the_geom from agroscope.feuchtflaechenpotential_kulturlandschaft) as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * FROM siedlung_landschaft.agglomerationsverkehr) as geometrie using unique bgdi_id using srid=2056"
    DATA "the_geom FROM siedlung_landschaft.alpenkonvention USING UNIQUE row_id USING srid=2056"
    DATA "the_geom from (SELECT bgdi_id, bfs_no, name, ch_bez_d, ch_bez_f, ch_code_hn, flaeche, kt_kz, kt_no, %geometry% as the_geom from siedlung_landschaft.bauzonen order by ch_code_hn) as geometrie using unique bgdi_id using srid=2056"
    DATA "the_geom FROM oeffentlicher_verkehr.belastung_gueterverkehr_bahn USING UNIQUE row_id USING srid=2056"
    DATA "the_geom FROM strassen.belastung_gueterverkehr_strasse USING UNIQUE row_id USING srid=2056"
    DATA "the_geom FROM oeffentlicher_verkehr.belastung_personenverkehr USING UNIQUE bgdi_id USING srid=2056"
    DATA "the_geom FROM oeffentlicher_verkehr.belastung_personenverkehr_zukunft USING UNIQUE bgdi_id USING srid=2056"
    DATA "the_geom FROM strassen.belastung_personenverkehr USING UNIQUE bgdi_id USING srid=2056"
    DATA "the_geom FROM strassen.belastung_personenverkehr_zukunft USING UNIQUE bgdi_id USING srid=2056"
    DATA "the_geom from (SELECT * from siedlung_landschaft.erreichbarkeit) as geometrie using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * from siedlung_landschaft.erreichbarkeit) as geometrie using unique bgdi_id using srid=2056"
    DATA "the_geom FROM siedlung_landschaft.gemeindetyp_1990_9klassen USING UNIQUE row_id USING srid=2056"
    DATA "the_geom from (SELECT * from siedlung_landschaft.gemeindetypologie_2012 order by typ_code) as geometrie using unique bgdi_id using srid=2056"
    DATA "the_geom FROM oeffentlicher_verkehr.gueteklassen USING UNIQUE id USING srid=2056"
    DATA "the_geom FROM siedlung_landschaft.landschaftstypen USING UNIQUE row_id USING srid=2056"
    DATA "the_geom FROM siedlung_landschaft.landschaftstypen USING UNIQUE row_id USING srid=2056"
    DATA "the_geom from (SELECT * from siedlung_landschaft.reisezeit_agglomerationen) as geometrie using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * from siedlung_landschaft.reisezeit_agglomerationen_punkte) as geometrie using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * from siedlung_landschaft.reisezeit_agglomerationen) as geometrie using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * from siedlung_landschaft.reisezeit_agglomerationen_punkte) as geometrie using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * from siedlung_landschaft.reisezeit) as geometrie using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * from siedlung_landschaft.reisezeit_agglomerationen_punkte) as geometrie using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * from siedlung_landschaft.reisezeit) as geometrie using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * from siedlung_landschaft.reisezeit_agglomerationen_punkte) as geometrie using unique bgdi_id using srid=2056"
    DATA "the_geom FROM siedlung_landschaft.windenergie_bundesint USING UNIQUE bgdi_id USING srid=2056"
    DATA "the_geom from (SELECT bgdi_id, gemeinde_name as gemeinde, gemeinde_nummer, verfahren, zwg_3010, zwg_3100, zwg_3110, zwg_3120, zwg_3150, status, the_geom from raumplanung.zweitwohnungsanteil) as pfff using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT bgdi_id, the_geom, stabil_id, text_aussage_%lang% as text_aussage, text_thema_%lang% as text_thema, text_rechtsstatus_%lang% as text_rechtsstatus, publiziertab, publiziertbis, text_amt_%lang% as text_amt from astra.baulinien_nationalstrassen_v2_0_oereb WHERE rechtsstatus IN ('AenderungOhneVorwirkung', 'AenderungMitVorwirkung')) as geometrie using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * from astra.baulinien_nationalstrassen_area) as geometrie using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT bgdi_id, status, approval_date, approving_authority as authority, publication_date_from as publication_date, planning_approval_name, status as state, the_geom from astra.baulinien_nationalstrassen_line) as geometrie using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT bgdi_id, the_geom, stabil_id, text_aussage_%lang% as text_aussage, text_thema_%lang% as text_thema, text_rechtsstatus_%lang% as text_rechtsstatus, publiziertab, publiziertbis, text_amt_%lang% as text_amt from astra.baulinien_nationalstrassen_v2_0_oereb WHERE rechtsstatus LIKE 'inKraft') as geometrie using unique bgdi_id using srid=2056"
    DATA "the_geom FROM (SELECT bgdi_id, roadnumber, segmentid, canton, the_geom from astra.hauptstrassennetz) AS foo USING UNIQUE bgdi_id USING srid=2056"
    DATA "the_geom from (SELECT the_geom, ivs_nummer, ivs_signatur_%lang% as ivs_signatur_label, ivs_kanton,
    DATA "the_geom from (SELECT the_geom, ivs_nummer, ivs_signatur_%lang% as ivs_signatur_label, ivs_kanton,
    DATA "the_geom from (SELECT the_geom, ivs_nummer, ivs_signatur_%lang% as ivs_signatur_label, ivs_kanton,
    DATA "the_geom from (SELECT full_number as id, title as chmobil_title, route_number as chmobil_route_number, has_segment as chmobil_has_segment, the_geom from astra.chmobil_mountainbikeland) as blabla using unique id using srid=2056"
    DATA "the_geom from (SELECT * FROM astra.sperrungen_umleitungen_line WHERE land ~~* '%mtbland%'::text) as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * from astra.sperrungen_umleitungen_point WHERE land ~~* '%mtbland%'::text AND (symbol = ANY (ARRAY['warning'::text, 'traffic_ban'::text, 'pedestrian_ban'::text, 'bicycle_ban'::text]))) as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom from astra.nsa USING UNIQUE bgdi_id USING srid=2056"
    DATA "the_geom from astra.nsa USING UNIQUE bgdi_id USING srid=2056"
    DATA "the_geom from (SELECT  bgdi_id, the_geom, stabil_id, text_aussage_%lang% as text_aussage, text_thema_%lang% as text_thema, text_rechtsstatus_%lang% as text_rechtsstatus, publiziertab, publiziertbis, text_amt_%lang% as text_amt from astra.projektierungszonen_nationalstrassen_v2_0_oereb) as data using unique stabil_id using srid=2056"
    DATA "the_geom from (SELECT * from astra.sachplan_strasse_pl_anhoerung WHERE geom_type like '%area%') as data using unique stabil_id using srid=2056"
    DATA "the_geom from (SELECT * from astra.sachplan_strasse_pl_r_anhoerung WHERE geom_type like '%area%') as data using unique stabil_id using srid=2056"
    DATA "the_geom from  (SELECT * from astra.sachplan_strasse_fac_anhoerung WHERE geom_type like '%line%') as data using unique stabil_id using srid=2056"
    DATA "the_geom from  (SELECT * from astra.sachplan_strasse_pl_anhoerung WHERE geom_type like '%line%') as data using unique stabil_id using srid=2056"
    DATA "the_geom from (SELECT * from astra.sachplan_strasse_fac_anhoerung WHERE geom_type like '%point%') as data using unique stabil_id using srid=2056"
    DATA "the_geom from (SELECT * from astra.sachplan_strasse_pl_anhoerung WHERE geom_type like '%point%') as data using unique stabil_id using srid=2056"
    DATA "the_geom from (SELECT * from astra.sachplan_strasse_pl_kraft WHERE geom_type like '%area%') as data using unique stabil_id using srid=2056"
    DATA "the_geom from (SELECT * from astra.sachplan_strasse_pl_r_kraft WHERE geom_type like '%area%') as data using unique stabil_id using srid=2056"
    DATA "the_geom from  (SELECT * from astra.sachplan_strasse_fac_kraft WHERE geom_type like '%line%') as data using unique stabil_id using srid=2056"
    DATA "the_geom from  (SELECT * from astra.sachplan_strasse_pl_kraft WHERE geom_type like '%line%') as data using unique stabil_id using srid=2056"
    DATA "the_geom from (SELECT * from astra.sachplan_strasse_fac_kraft WHERE geom_type like '%point%') as data using unique stabil_id using srid=2056"
    DATA "the_geom from (SELECT * from astra.sachplan_strasse_pl_kraft WHERE geom_type like '%point%') as data using unique stabil_id using srid=2056"
    DATA "the_geom from (SELECT bgdi_id,canton,year,population,accalcohol_ugt_usv,accalcohol_ugt,accalcohol_usv,accalcohol_ugt_usv_perpopulation,the_geom  from astra.unf_schwer) as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT the_geom,bgdi_id,canton,year,population,accspeed_ugt_usv,accspeed_ugt,accspeed_usv,accspeed_ugt_usv_perpopulation from astra.unf_schwer) as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT the_geom,bgdi_id,canton,year,acc_ugt,acc_usv,acc_ugt_usv,acc_ugt_lastyear,acc_usv_lastyear,acc_ugt_usv_lastyear,acc_ugt_usv_yearchangepercent from astra.unf_schwer) as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT the_geom,bgdi_id,canton,year,population,acc_ugt_usv,acc_ugt,acc_usv,acc_ugt_usv_perpopulation from astra.unf_schwer) as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT full_number as id, title as chmobil_title, route_number as chmobil_route_number, has_segment as chmobil_has_segment, the_geom from astra.chmobil_skatingland) as blabla using unique id using srid=2056"
    DATA "the_geom from (SELECT * FROM astra.sperrungen_umleitungen_line WHERE land ~~* '%skatingland%'::text) as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * from astra.sperrungen_umleitungen_point WHERE land ~~* '%skatingland%'::text AND (symbol = ANY (ARRAY['warning'::text, 'traffic_ban'::text, 'pedestrian_ban'::text, 'bicycle_ban'::text]))) as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom FROM (SELECT the_geom, nr, code_status AS status, code_netz AS netz FROM astra.verkehrszaehlstellen WHERE (messstellentyp LIKE '%nur Online%' OR messstellentyp LIKE '%Online+Statistik%')) AS blabla using unique nr using srid=2056"
    DATA "the_geom FROM (SELECT the_geom, nr, code_status AS status, code_netz AS netz FROM astra.verkehrszaehlstellen WHERE (messstellentyp LIKE '%nur Statistik%' OR messstellentyp LIKE '%Online+Statistik%')) AS blabla using unique nr using srid=2056"
    DATA "the_geom from (SELECT * from astra.strassenverkehrszaehlung_uebergeordnet) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from  (
    DATA "the_geom from  (
    DATA "the_geom from  (
    DATA "the_geom from  (
    DATA "the_geom from  (
    DATA "the_geom from (SELECT full_number as id, title as chmobil_title, route_number as chmobil_route_number, has_segment as chmobil_has_segment, the_geom from astra.chmobil_veloland) as blabla using unique id using srid=2056"
    DATA "the_geom from (SELECT * FROM astra.sperrungen_umleitungen_line WHERE land ~~* '%veloland%'::text) as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * from astra.sperrungen_umleitungen_point WHERE land ~~* '%veloland%'::text AND (symbol = ANY (ARRAY['warning'::text, 'traffic_ban'::text, 'pedestrian_ban'::text, 'bicycle_ban'::text]))) as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT full_number as id, title as chmobil_title, route_number as chmobil_route_number, has_segment as chmobil_has_segment, the_geom from astra.chmobil_wanderland) as blabla using unique id using srid=2056"
    DATA "the_geom from (SELECT * FROM astra.sperrungen_umleitungen_line WHERE land ~~* '%wanderland%'::text) as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * from astra.sperrungen_umleitungen_point WHERE land ~~* '%wanderland%'::text AND (symbol = ANY (ARRAY['warning'::text, 'traffic_ban'::text, 'pedestrian_ban'::text, 'bicycle_ban'::text]))) as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom_polygon FROM (SELECT *  FROM babs.kgs ) AS kultur USING UNIQUE bgdi_id USING SRID=2056"
    DATA "the_geom FROM (SELECT *  FROM babs.kgs ) AS kultur USING UNIQUE bgdi_id USING SRID=2056"
    DATA "the_geom from (SELECT bgdi_id,kontemail,kontname,konttel,name,code_refverhalten,code_hundepraesenz,code_hinweis,the_geom from fauna.alpenweiden_herdenschutzhunde) as geometrie using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT nr_datenbank, the_geom, nr_kanton, name, type_de, type_fr, type_it, massnahme_de, massnahme_fr, massnahme_it, ref_datenblatt from fauna.amphibienwanderung_verkehrskonflikte) as data using unique nr_datenbank using srid=2056"
    DATA "the_geom from schutzge.ak"
    DATA "the_geom from (SELECT *  from wasser.aquaprotect_050) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT *  from wasser.aquaprotect_100) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT *  from wasser.aquaprotect_250) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT *  from wasser.aquaprotect_500) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * FROM schutzge.alpinauen) as geometrie using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * FROM schutzge.auen_ausserhalb_bundinv) as geometrie using unique bgdi_id using srid=2056"
    DATA "the_geom FROM (SELECT code_primaervegetation::NUMERIC::INTEGER as classitem, * FROM flora.auen_vegetationskarte) AS data using unique bgdi_id using srid=2056"
    DATA "the_geom from  (SELECT * from diverse.biogeoreg) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from  (SELECT bgdi_id, the_geom, name, objnummer, obnr, refobjblat from bundinv.amphibien_anhang4) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from bundinv.am_l"
    DATA "the_geom from (SELECT %geometry% as the_geom, objnummer, name, refobjblat, shape_area / 10000 AS shape_area, bgdi_id, site, site_de, site_fr, site_it from bundinv.amphibien) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from bundinv.amphibien_wanderobjekte USING UNIQUE bgdi_id USING srid=2056"
    DATA "the_geom from (
    DATA "the_geom from (
    DATA "the_geom from (
    DATA "the_geom from bundinv.bln_v2"
    DATA "the_geom from bundinv.bln"
    DATA "the_geom from (SELECT  %geometry% as the_geom, bgdi_id, objnummer, name, shape_area / 10000 as shape_area, refobjblat from bundinv.flachmoore) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (
    DATA "the_geom from bundinv.jb"
    DATA "the_geom from (SELECT  %geometry% as the_geom, bgdi_id, objnummer, name, shape_area, refobjblat from bundinv.moorlandschaften ) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT bgdi_id,name,objnummer,refobjblat,shape_area,teilobjnummer,bewertungs, %geometry% as the_geom  from bundinv.trockenwiesen_anhang2) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT %geometry% as the_geom, bgdi_id, name, objnummer, refobjblat, teilobjnummer, bewertungs, shape_area / 10000 as shape_area from bundinv.trockenwiesen) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from bundinv.vogelreservate"
    DATA "the_geom from fauna.ch_achna"
    DATA "the_geom from fauna.ko_natio"
    DATA "the_geom from (SELECT gid, code, code_lookup, ren_vers, the_geom from ren.aqua) as blabla using unique gid using srid=2056"
    DATA "the_geom from (SELECT gid, code, code_lookup, ren_vers, the_geom from ren.ext) as blabla using unique gid using srid=2056"
    DATA "the_geom from (SELECT gid, code, code_lookup, ren_vers, the_geom from ren.humide) as blabla using unique gid using srid=2056"
    DATA "the_geom from (SELECT gid, code, code_lookup, ren_vers, the_geom  from ren.seche) as blabla using unique gid using srid=2056"
    DATA "the_geom from ( SELECT bgdi_id, the_geom, name, objnummer, kantone, shape_area, gebietstyp, gebietstyp_de, gebietstyp_fr,  gebietstyp_it from fauna.steinbockkolonien) as nofx using unique bgdi_id using srid=2056"
    DATA "the_geom from  (SELECT *  from fauna.vernetzungsachsen) as data using unique _count using srid=2056"
    DATA "the_geom from (SELECT the_geom, bgdi_id ,objnummer ,name ,zustand ,ref_obj_blat ,zustand_de ,zustand_fr ,zustand_it from fauna.wildtierkorridore_national) as data using unique bgdi_id using srid=2056"
    DATA "the_geom FROM (SELECT * FROM fauna.wildtierpassagen) AS bla USING UNIQUE bgdi_id USING SRID=2056"
    DATA "the_geom from  (SELECT *  from wasser.geschiebemessnetz) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from  (SELECT bgdi_id, gewaesser, aesch_vers, the_geom  from fischerei.aeschen_kernzonen) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from  (SELECT bgdi_id, gewaesser, aesch_vers, the_geom  from fischerei.aeschen_laichplaetze) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from  (SELECT bgdi_id, gewaesser, aesch_vers, the_geom  from fischerei.aeschen_larven) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from  (SELECT bgdi_id, gewaesser, aesch_ver, the_geom  from fischerei.aeschen_verbreitung) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from  (SELECT *  from fischerei.krebspest) as data using unique _count using srid=2056"
    DATA "the_geom from  (SELECT bgdi_id, laich_anz, the_geom  from fischerei.nasenlaichplaetze order by laich_anz desc) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from  (SELECT *  from fischerei.proliferative_nierenkrankheit) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from flora.auen_veg"
    DATA "the_geom from  (SELECT *  from flora.schwingrasen) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT *  from flora.raster_verbreitungskarten) as data using unique gid using srid=2056"
    DATA "the_geom from (SELECT *  from flora.ws) as data using unique gid using srid=2056"
    DATA "the_geom FROM (SELECT * FROM wasser.flussordnungszahlen) as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * from gefahren.basiskarte_countries) as data using unique id using srid=2056"
    DATA "the_geom from (SELECT * from gefahren.basiskarte_lakes) as data using unique id using srid=2056"
    DATA "the_geom from (SELECT * from gefahren.basiskarte_coasts) as data using unique id using srid=2056"
    DATA "the_geom from (SELECT * from gefahren.basiskarte_rivers) as data using unique id using srid=2056"
    DATA "the_geom from (SELECT the_geom, bgk, referenz, bgdi_id FROM gefahren.baugrundklassen) as blabla USING UNIQUE bgdi_id USING srid=2056"
    DATA "the_geom from (SELECT * FROM gefahren.gefaehrdungszonen) as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * from gefahren.historische_erdbeben) as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT *  from gefahren.mikrozonierungen) as data using unique _count using srid=2056"
    DATA "the_geom from (SELECT * from gefahren.baugrundkl_spectral) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT bgdi_id, level, region_id, name_%lang% as name, title_%lang% as title, description_%lang% as description, valid_from, canton, the_geom FROM gefahren.waldbrandpraevention) as geometrie using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT bgdi_id, region_id, name_%lang% as name, title_%lang% as title, valid_from, level, canton, the_geom FROM gefahren.waldbrandwarnung) as geometrie using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * from wasser.baqua) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from  (SELECT id_nawa, klasse, bgdi_id, (jahr::text || '-01-01'::text)::date AS bgdi_date, gewaesser_ as gewaesser,jahr,kanton,stelle_neu,klasse_de,klasse_fr,klasse_it,klasse_en, the_geom from wasser.gewaesserschutz_diatomeen) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from  (SELECT id_nawa, klasse, bgdi_id, (jahr::text || '-01-01'::text)::date AS bgdi_date, gewaesser_ as gewaesser,jahr,kanton,stelle_neu,klasse_de,klasse_fr,klasse_it,klasse_en, the_geom from wasser.gewaesserschutz_fische) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from  (SELECT id_nawa, klasse, bgdi_id, (jahr::text || '-01-01'::text)::date AS bgdi_date, gewaesser_ as gewaesser,jahr,kanton,stelle_neu,klasse_de,klasse_fr,klasse_it,klasse_en, the_geom from wasser.gewaesserschutz_makrophyten) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from  (SELECT id_nawa, klasse, bgdi_id, (jahr::text || '-01-01'::text)::date AS bgdi_date, gewaesser_ as gewaesser,jahr,kanton,stelle_neu,klasse_de,klasse_fr,klasse_it,klasse_en, the_geom from wasser.gewaessweschutz_makrozoobenthos) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from  (SELECT id_nawa, klasse, bgdi_id, (jahr::text || '-01-01'::text)::date AS bgdi_date, gewaesser_ as gewaesser,jahr,kanton,stelle_neu,klasse_de,klasse_fr,klasse_it,klasse_en, the_geom from wasser.gewaesserschutz_ammonium) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from  (SELECT id_nawa, klasse, bgdi_id, (jahr::text || '-01-01'::text)::date AS bgdi_date, gewaesser_ as gewaesser,jahr,kanton,stelle_neu,klasse_de,klasse_fr,klasse_it,klasse_en, the_geom from wasser.gewaesserschutz_doc) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from  (SELECT id_nawa, klasse, bgdi_id, (jahr::text || '-01-01'::text)::date AS bgdi_date, gewaesser_ as gewaesser,jahr,kanton,stelle_neu,klasse_de,klasse_fr,klasse_it,klasse_en, the_geom from wasser.gewaesserschutz_nitrat) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from  (SELECT id_nawa, klasse, bgdi_id, (jahr::text || '-01-01'::text)::date AS bgdi_date, gewaesser_ as gewaesser,jahr,kanton,stelle_neu,klasse_de,klasse_fr,klasse_it,klasse_en, the_geom from wasser.gewaesserschutz_nitrit) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from  (SELECT id_nawa, klasse, bgdi_id, (jahr::text || '-01-01'::text)::date AS bgdi_date, gewaesser_ as gewaesser,jahr,kanton,stelle_neu,klasse_de,klasse_fr,klasse_it,klasse_en, the_geom from wasser.gewaesserschutz_phosphat) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from  (SELECT id_nawa, klasse, bgdi_id, (jahr::text || '-01-01'::text)::date AS bgdi_date, gewaesser_ as gewaesser,jahr,kanton,stelle_neu,klasse_de,klasse_fr,klasse_it,klasse_en, the_geom from wasser.gewaesserschutz_phosphor_gesamt) as data using unique bgdi_id using srid=2056"
    DATA "the_geom FROM (SELECT * FROM wasser.klaeranlagen) as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom FROM (SELECT * FROM wasser.klaeranlagen) as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom FROM (SELECT * FROM wasser.klaeranlagen) as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom from (select bgdi_id, name, gwl_nr, bestockung, %geometry% as the_geom from wasser.gewaesser_uferbestockung) as bla using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT %geometry% as the_geom, farbe, featureclass, bgdi_id,area,flussgebiet,grundwasserleitertyp,grundwasserregime,gwkid,gwkname,naturraum from wasser.gwk) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from  (SELECT id, the_geom,datum,markierstoff,menge_einheit,milieu,nummer_ein,ort,x,y  from hydrogeolog.hydrogeologie_markierversuche)  as data using unique id using srid=2056"
    DATA "the_geom from (SELECT *  from hydrogeolog.hydrogeolog_uebersichtskarte WHERE datasource like '%vector%') as data using unique id using srid=2056"
    DATA "the_geom from (SELECT * from hydrogeolog.hydrogeolog_uebersichtskarte WHERE datasource like '%raster%') as data using unique id using srid=2056"
    DATA "the_geom from  (SELECT the_geom, bgdi_id, name,rechtswert,hochwert,hoehe,betriebsbeginn,einzugsgebietsflaeche,mittlerehoehe,vergletscherung,stationierung,flussgebiet,hyperlink_daten,hyperlink_d,hyperlink_f from hydrologie.hydro_duntersuchung_fliessgewaesser) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT nr, gewaesser, name, bgdi_id, the_geom from hydrologie.dbgz) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * from hydrologie.hydrologie_hintergrundkarte where st_geometrytype(the_geom) like '%olygon%') as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * from hydrologie.hydrologie_hintergrundkarte where st_geometrytype(the_geom) like '%olygon%') as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * from hydrologie.hydrologie_hintergrundkarte where st_geometrytype(the_geom) like '%ine%') as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * from hydrologie.hydrologie_hintergrundkarte where st_geometrytype(the_geom) like '%oint%') as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT objectid, datenherkunft, nummer, name, rechtswert, hochwert, hoehe, einzugsgebietsflaeche, fluss, m_beginn, m_ende, the_geom from hydrologie.hochwassergrenzwertpegel) as data using unique objectid using srid=2056"
    DATA "the_geom from (SELECT the_geom,bgdi_id,name,url_fr,url_de,url_hqpdf  from hydrologie.hochwasserstatistik) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from hydrologie.lhg"
    DATA "the_geom from (SELECT *  from hydrologie.niedrigwasserstatistik) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT *  from hydrologie.hydro_q347) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT *  from hydrologie.hydrologie_untersuchungsgebiete) as data using unique geodb_oid using srid=2056"
    DATA "the_geom from (SELECT *  from hydrologie.hydrologie_untersuchungsgebiete_stationen) as data using unique geodb_oid using srid=2056"
    DATA "the_geom from (SELECT nr, url, name, the_geom from hydrologie.temperaturmessnetz) as data using unique nr using srid=2056"
    DATA "the_geom from  (SELECT bgdi_id as id, * from hydrologie.atlas_basisgebiete) as data using unique id using srid=2056"
    DATA "the_geom from  (SELECT bgdi_id as id, * from hydrologie.atlas_bilanzgebiete) as data using unique id using srid=2056"
    DATA "the_geom from  (SELECT bgdi_id as id, * from hydrologie.atlas_flussgebiete) as data using unique id using srid=2056"
    DATA "the_geom from  (SELECT bgdi_id as id, * from hydrologie.hydro_atlas_kant_messstationen) as data using unique id using srid=2056"
    DATA "the_geom from (SELECT *  from hydrologie.karst_grundwasservorkommen) as data using unique objectid using srid=2056"
    DATA "the_geom from (SELECT *  from hydrologie.karst_einzugsgebiete) as data using unique objectid using srid=2056"
    DATA "the_geom from (SELECT *  from hydrologie.karst_einzugsgebieteinheiten) as data using unique objectid using srid=2056"
    DATA "the_geom from (SELECT *  from hydrologie.karst_quellen_schwinden) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT *  from hydrologie.karst_fliesswege) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from  (SELECT * from klima.co2_ausstoss_gebaeude) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (select * from diverse.landesforstinventar_kantone) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * from wald.produktionsregionen) as data using unique id using srid=2056"
    DATA "the_geom from (SELECT * from wald.schutzwaldregionen) as data using unique id using srid=2056"
    DATA "the_geom FROM (SELECT bgdi_id, the_geom, datenstand from wald.vegetationshoehenmodell) as data using unique bgdi_id using srid=2056"
    DATA "the_geom FROM (SELECT bgdi_id, the_geom, datenstand from wald.vegetationshoehenmodell) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * from wald.wirtschaftsregionen) as data using unique id using srid=2056"
    DATA "the_geom FROM (SELECT typoch_group, bgdi_id, typoch_de, typoch_fr, typoch_it, prob_de, prob_fr, prob_it, cover, the_geom FROM lebensraumkarte.lebensraumkarte_schweiz) AS bla USING UNIQUE bgdi_id USING srid=2056"
    DATA "the_geom FROM (SELECT typoch_group, bgdi_id, the_geom FROM lebensraumkarte.lebensraumkarte_schweiz) AS bla USING UNIQUE bgdi_id USING srid=2056"
    DATA "the_geom FROM luft.ammoniakkonzentration USING UNIQUE bgdi_id USING srid=2056"
    DATA "the_geom FROM luft.stickstoffdeposition USING UNIQUE bgdi_id USING srid=2056"
    DATA "the_geom FROM luft.stickstoff_kritischer_eintrag USING UNIQUE bgdi_id USING srid=2056"
    DATA "the_geom from  (SELECT *  from wasser.mittlere_abfluesse) as date using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT *, kreis * 2.5 as kreis_scaled from wasser.mittlere_abfluesse_zukunft) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT bgdi_id,genus,jahr,nhv_text,populationsnr,rl_text,standort,the_geom from flora.mooseflora) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from  (SELECT id_stat, the_geom, gid, name, desc_de, desc_fr, url, typ_de, typ_fr, wert_url_d, wert_url_f FROM luft.nabel) as blabla USING UNIQUE gid USING srid=2056"
    DATA "the_geom from  (SELECT bgdi_id, no3____max, the_geom  from wasser.naqua_nitrat_konz) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT %geometry% as the_geom, farbe, featureclass, bgdi_id from wasser.gwk) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from  (SELECT bgdi_id, maximum, the_geom  from wasser.naqua_fluecht_org_verb) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT %geometry% as the_geom, farbe, featureclass, bgdi_id from wasser.gwk) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from  (SELECT *  from wasser.oekom_abschnitte) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * from wasser.oekom_abstuerze) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from  (SELECT *  from wasser.oekom_bauwerke) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from ren.ext using unique gid using srid=2056"
    DATA "the_geom from (SELECT gid, code, code_lookup, ren_vers, the_geom from ren.humide) as blabla using unique gid using srid=2056"
    DATA "the_geom from (SELECT gid, code, code_lookup, ren_vers, the_geom from ren.aqua) as blabla using unique gid using srid=2056"
    DATA "the_geom from ren.seche using unique gid using srid=2056"
    DATA "the_geom_gen25 from ren.foret using unique gid using srid=2056"
    DATA "the_geom from (SELECT * from wasser.invent_ent_andere_bedeutend) as blabla using unique gid using srid=2056"
    DATA "the_geom from (SELECT * from wasser.invent_ent_andere_weitere) as blabla using unique gid using srid=2056"
    DATA "the_geom from (SELECT * from wasser.entnahme) as blabla using unique gid using srid=2056"
    DATA "the_geom from (SELECT * from wasser.invent_ent_wknutz_bedeutend) as blabla using unique gid using srid=2056"
    DATA "the_geom from (SELECT * from wasser.invent_ent_wknutz_weitere) as blabla using unique gid using srid=2056"
    DATA "the_geom from wasser.leitungen"
    DATA "the_geom from wasser.rueckgabe"
    DATA "the_geom from (select bgdi_id, the_geom from schutzge.auengebiete_general) as blabla USING UNIQUE bgdi_id USING srid=2056"
    DATA "the_geom from (select bgdi_id, objnummer, name, discriminator, the_geom from schutzge.auen_gebiete) as blabla USING UNIQUE bgdi_id USING srid=2056"
    DATA "the_geom from (select bgdi_id, the_geom from schutzge.jagdbanngebiete_general) as blabla USING UNIQUE bgdi_id USING srid=2056"
    DATA "the_geom from (select bgdi_id,objnummer,name,discriminator, the_geom from schutzge.jagdbann_gebiete) as blabla USING UNIQUE bgdi_id USING srid=2056"
    DATA "the_geom from (select bgdi_id, the_geom from schutzge.moorlandschaften_general) as blabla USING UNIQUE bgdi_id USING srid=2056"
    DATA "the_geom from (select  bgdi_id,objnummer,name,discriminator, the_geom from schutzge.moor_landschaften) as blabla USING UNIQUE bgdi_id USING srid=2056"
    DATA "the_geom from (select bgdi_id, the_geom from schutzge.uebrige_gebiete_general) as blabla USING UNIQUE bgdi_id USING srid=2056"
    DATA "the_geom from (select bgdi_id,objnummer,name,discriminator, the_geom from schutzge.uebrigegebiete) as blabla USING UNIQUE bgdi_id USING srid=2056"
    DATA "the_geom from schutzge.biores"
    DATA "the_geom from (SELECT the_geom, zone, bgdi_id, name, objnummer, version, shape_area_ha from schutzge.biosphaeren) AS bla USING UNIQUE bgdi_id USING SRID=2056"
    DATA "the_geom from (SELECT * from schutzge.luftfahrt order by kategorie desc) as data using unique bgdi_id srid=2056"
    DATA "the_geom from schutzge.nat_park"
    DATA "the_geom from (SELECT bgdi_id, zone, teil_nummer, objektnummer, shape_area, park_name as name, kategorie, rechtsgrundlage, status, the_geom from schutzge.paerke_nationaler_bedeutung_zonen) as blabla using unique bgdi_id srid=2056"
    DATA "the_geom from (SELECT objektnummer, status, kategorie, shape_area, park_name as name, rechtsgrundlage, the_geom from schutzge.paerke_nationaler_bedeutung_perimeter) as blabla using unique objektnummer srid=2056"
    DATA "the_geom from schutzge.ramsar"
    DATA "the_geom from schutzge.rb"
    DATA "the_geom from (SELECT * from schutzge.smaragd) as blabla using unique id using srid=2056"
    DATA "the_geom from schutzge.vaew"
    DATA "the_geom from schutzge.wne_unesco"
    DATA "the_geom from (SELECT id, bgdi_id, %geometry% as the_geom  from silvaprotect.hangmure) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT id, bgdi_id, %geometry% as the_geom  from silvaprotect.lawinen) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT id, bgdi_id, %geometry% as the_geom  from silvaprotect.murgang) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT id, bgdi_id, %geometry% as the_geom  from silvaprotect.sturz) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT id, bgdi_id, gridcode, %geometry% as the_geom  from silvaprotect.uebersarung) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT *  from wasser.strukturguete_hochrhein) as data using unique geodb_oid using srid=2056"
    DATA "the_geom from (SELECT *  from wasser.strukturguete_hochrhein) as data using unique geodb_oid using srid=2056"
    DATA "the_geom from (SELECT *  from wasser.strukturguete_hochrhein) as data using unique geodb_oid using srid=2056"
    DATA "the_geom from (SELECT *  from wasser.strukturguete_hochrhein) as data using unique geodb_oid using srid=2056"
    DATA "the_geom from (SELECT *  from wasser.strukturguete_hochrhein) as data using unique geodb_oid using srid=2056"
    DATA "the_geom from (SELECT *  from diverse.data_boeenspitzen) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT *  from diverse.data_boeenspitzen) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT *  from diverse.data_boeenspitzen) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT *  from diverse.data_boeenspitzen) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT *  from diverse.data_staudruck) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT *  from diverse.data_staudruck) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT *  from diverse.data_staudruck) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT *  from diverse.data_staudruck) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from  (SELECT *  from prtr.swissprtr) as data using unique prtrnr using srid=2056"
    DATA "the_geom from (select * from schutzge.tranquillity_gebiete) as blabla USING UNIQUE bgdi_id USING srid=2056"
    DATA "the_geom from (SELECT *  from hydrologie.typisierung_fliessgewaesser) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from schutzge.wne_unesco"
    DATA "the_geom from  (SELECT * from vec25.gewaessernetz_2000) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from  (SELECT * from vec25.seen) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * from wald.waldreservate) as geometrie using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT *  from schadenflaechen.lothar25) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT *  from schadenflaechen.vivian25) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * from wald.waldstandortsregionen) AS bla USING UNIQUE bgdi_id USING SRID=2056"
    DATA "the_geom from (SELECT * from wald.waldtannenareale) AS bla USING UNIQUE bgdi_id USING SRID=2056"
    DATA "the_geom from (SELECT * from wald.vegetationshoehenstufen_1975) AS bla USING UNIQUE bgdi_id USING SRID=2056"
    DATA "the_geom from (SELECT * from wald.vegetationshoehenstufen_2085_trocken) AS bla USING UNIQUE bgdi_id USING SRID=2056"
    DATA "the_geom from (SELECT * from wald.vegetationshoehenstufen_2085_weniger_trocken) AS bla USING UNIQUE bgdi_id USING SRID=2056"
    DATA "the_geom from (SELECT * from wasser.querprofilmarken) as data using unique bgdi_id using srid=2056"
    DATA "the_geom_li from (SELECT * from wasser.querprofilmarken) as data using unique bgdi_id using srid=2056"
    DATA "the_geom_re from (SELECT * from wasser.querprofilmarken) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT *  from wasser.vermessungsabschnitte) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * from wasser.invent_ent_wknutz_bedeutend) as blabla using unique gid using srid=2056"
    DATA "the_geom from (SELECT * from wasser.invent_ent_wknutz_weitere) as blabla using unique gid using srid=2056"
    DATA "the_geom from (SELECT * from wasser.invent_ent_andere_bedeutend) as blabla using unique gid using srid=2056"
    DATA "the_geom from (SELECT * from wasser.invent_ent_andere_weitere) as blabla using unique gid using srid=2056"
    DATA "the_geom from (SELECT * from wasser.gebietsauslaesse) as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom from wasser.leitungen"
    DATA "the_geom from wasser.rueckgabe"
    DATA "the_geom from (
    DATA "the_geom from (SELECT * from wasser.ebene_40km) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * from wasser.effluent) as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom FROM (SELECT * FROM wrzportal.jgd_select_flaechen) as blabla using unique objectid using srid=2056"
    DATA "the_geom FROM (SELECT * FROM wrzportal.jgd_select_wege) as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom FROM (
    DATA "the_geom FROM (SELECT * FROM wrzportal.wrz_portal_flaechen) as blabla using unique objectid using srid=2056"
    DATA "the_geom FROM (SELECT * FROM wrzportal.wrz_portal_wege) as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom FROM (
    DATA "the_geom FROM (SELECT bgdi_id as id, * FROM bag.radioaktivitaet) AS bla USING UNIQUE id USING srid=2056"
    DATA "the_geom from (SELECT bgdi_id as id, * from bag.radonkarte) as geometrie using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * from bag.fsme_faelle) as geometrie using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * from bag.fsme_impfung) as geometrie using unique bgdi_id using srid=2056"
    DATA "the_geom FROM (SELECT xtf_id, the_geom, kantone, id as nummer, name, siedlungskategorie, url FROM view_isos_foto) as bla USING UNIQUE xtf_id USING SRID=2056"
    DATA "the_geom FROM (SELECT xtf_id, bild_typ, 360 - azimut as angle, SIN((360 - azimut)*PI()/180)*10 AS offset_x, COS((360 - azimut)*PI()/180)*10 AS offset_y, the_geom, kantone, id as nummer, name, siedlungskategorie, url FROM view_isos_foto_bild) as bla USING UNIQUE xtf_id USING SRID=2056"
    DATA "the_geom FROM (SELECT xtf_id, siedlungskategorie_code, %size%::int as size, the_geom, kantone, id as nummer, name, siedlungskategorie, url FROM isos_ortsbild %filter%) as bla USING UNIQUE xtf_id USING SRID=2056"
    DATA "the_geom FROM (SELECT xtf_id, %size%::int as size, kantone, id as nummer, name, siedlungskategorie, url, %filter%) as bla USING UNIQUE xtf_id USING SRID=2056"
    DATA "the_geom FROM (SELECT xtf_id, typ, the_geom, kantone, id as nummer, name, siedlungskategorie, url, hinweis_id as hinweis_nummer, hinweis_name, siehe_auch, teil_id as teil_nummer, teil_name FROM view_isos_hinweis WHERE st_geometrytype(the_geom) = 'ST_MultiPoint') as bla USING UNIQUE xtf_id USING SRID=2056"
    DATA "the_geom FROM (SELECT xtf_id, typ, the_geom, kantone, id as nummer, name, siedlungskategorie, url, hinweis_id as hinweis_nummer, hinweis_name, siehe_auch, teil_id as teil_nummer, teil_name FROM view_isos_hinweis WHERE st_geometrytype(the_geom) != 'ST_MultiPoint') as bla USING UNIQUE xtf_id USING SRID=2056"
    DATA "the_geom FROM (SELECT * FROM public.halteplaetze_jenische_sinti_roma) as bla USING UNIQUE bgdi_id USING SRID=2056"
    DATA "the_geom FROM (SELECT cellid, alias, fdaurl, nbofprovider, the_geom FROM bakom.nga_anbieter) as blabla using unique cellid using srid=2056"
    DATA "the_geom from bakom.tv_gebiet"
    DATA "the_geom from bakom.ukw_gebiet"
    DATA "the_geom from (SELECT the_geom, bfs_nummer, objektart, name, festnetz_112, fn_gemeinde_112, fn_addresse_112, fn_rgb_112 from bakom.notruf order by st_area(the_geom) DESC) AS bla USING UNIQUE bfs_nummer USING SRID=2056"
    DATA "the_geom from (SELECT the_geom, bfs_nummer, objektart, name, festnetz_112, fn_gemeinde_112, fn_addresse_112, fn_rgb_112 from bakom.notruf WHERE objektart = 'Sondergebiet' order by st_area(the_geom) DESC) AS bla USING UNIQUE bfs_nummer USING SRID=2056"
    DATA "the_geom from (SELECT the_geom, bfs_nummer, objektart, name, mobile_112, mo_gemeinde_112, mo_addresse_112, mo_rgb_112 from bakom.notruf order by st_area(the_geom) DESC) AS bla USING UNIQUE bfs_nummer USING SRID=2056"
    DATA "the_geom from (SELECT the_geom, bfs_nummer, objektart, name, mobile_112, mo_gemeinde_112, mo_addresse_112, mo_rgb_112 from bakom.notruf where objektart = 'Sondergebiet' order by st_area(the_geom) DESC) AS bla USING UNIQUE bfs_nummer USING SRID=2056"
    DATA "the_geom from (SELECT the_geom, bfs_nummer, objektart, name, satellit_112, sa_gemeinde_112, sa_addresse_112, sa_rgb_112 from bakom.notruf order by display_order asc, st_area(the_geom) DESC) AS bla USING UNIQUE bfs_nummer USING SRID=2056"
    DATA "the_geom from (SELECT the_geom, bfs_nummer, objektart, name, satellit_112, sa_gemeinde_112, sa_addresse_112, sa_rgb_112 from bakom.notruf WHERE objektart = 'Sondergebiet' order by display_order asc, st_area(the_geom) DESC) AS bla USING UNIQUE bfs_nummer USING SRID=2056"
    DATA "the_geom from (SELECT the_geom, bfs_nummer, objektart, name, gemeinde_112, festnetz_112, fn_zentrale_112, mobile_112, mo_zentrale_112, poly_rgb_112 from bakom.notruf order by st_area(the_geom) DESC) AS bla USING UNIQUE bfs_nummer USING SRID=2056"
    DATA "the_geom from (SELECT the_geom, bfs_nummer, objektart, name, gemeinde_112, festnetz_112, fn_zentrale_112, mobile_112, mo_zentrale_112, poly_rgb_112 from bakom.notruf WHERE objektart = 'Sondergebiet' order by st_area(the_geom) DESC) AS bla USING UNIQUE bfs_nummer USING SRID=2056"
    DATA "the_geom from (SELECT the_geom, bfs_nummer, objektart, name, festnetz_117, fn_gemeinde_117, fn_addresse_117, fn_rgb_117 from bakom.notruf order by st_area(the_geom) DESC) AS bla USING UNIQUE bfs_nummer USING SRID=2056"
    DATA "the_geom from (SELECT the_geom, bfs_nummer, objektart, name, festnetz_117, fn_gemeinde_117, fn_addresse_117, fn_rgb_117 from bakom.notruf WHERE objektart = 'Sondergebiet' order by st_area(the_geom) DESC) AS bla USING UNIQUE bfs_nummer USING SRID=2056"
    DATA "the_geom from (SELECT the_geom, bfs_nummer, objektart, name, mobile_117, mo_gemeinde_117, mo_addresse_117, mo_rgb_117 from bakom.notruf order by st_area(the_geom) DESC) AS bla USING UNIQUE bfs_nummer USING SRID=2056"
    DATA "the_geom from (SELECT the_geom, bfs_nummer, objektart, name, mobile_117, mo_gemeinde_117, mo_addresse_117, mo_rgb_117 from bakom.notruf where objektart = 'Sondergebiet' order by st_area(the_geom) DESC) AS bla USING UNIQUE bfs_nummer USING SRID=2056"
    DATA "the_geom from (SELECT the_geom, bfs_nummer, objektart, name, gemeinde_117, festnetz_117, fn_zentrale_117, mobile_117, mo_zentrale_117, poly_rgb_117 from bakom.notruf order by st_area(the_geom) DESC) AS bla USING UNIQUE bfs_nummer USING SRID=2056"
    DATA "the_geom from (SELECT the_geom, bfs_nummer, objektart, name, gemeinde_117, festnetz_117, fn_zentrale_117, mobile_117, mo_zentrale_117, poly_rgb_117 from bakom.notruf WHERE objektart = 'Sondergebiet' order by st_area(the_geom) DESC) AS bla USING UNIQUE bfs_nummer USING SRID=2056"
    DATA "the_geom from (SELECT the_geom, bfs_nummer, objektart, name, festnetz_118, fn_gemeinde_118, fn_addresse_118, fn_rgb_118 from bakom.notruf order by st_area(the_geom) DESC) AS bla USING UNIQUE bfs_nummer USING SRID=2056"
    DATA "the_geom from (SELECT the_geom, bfs_nummer, objektart, name, festnetz_118, fn_gemeinde_118, fn_addresse_118, fn_rgb_118 from bakom.notruf WHERE objektart = 'Sondergebiet' order by st_area(the_geom) DESC) AS bla USING UNIQUE bfs_nummer USING SRID=2056"
    DATA "the_geom from (SELECT the_geom, bfs_nummer, objektart, name, mobile_118, mo_gemeinde_118, mo_addresse_118, mo_rgb_118 from bakom.notruf order by st_area(the_geom) DESC) AS bla USING UNIQUE bfs_nummer USING SRID=2056"
    DATA "the_geom from (SELECT the_geom, bfs_nummer, objektart, name, mobile_118, mo_gemeinde_118, mo_addresse_118, mo_rgb_118 from bakom.notruf where objektart = 'Sondergebiet' order by st_area(the_geom) DESC) AS bla USING UNIQUE bfs_nummer USING SRID=2056"
    DATA "the_geom from (SELECT the_geom, bfs_nummer, objektart, name, gemeinde_118, festnetz_118, fn_zentrale_118, mobile_118, mo_zentrale_118, poly_rgb_118 from bakom.notruf order by st_area(the_geom) DESC) AS bla USING UNIQUE bfs_nummer USING SRID=2056"
    DATA "the_geom from (SELECT the_geom, bfs_nummer, objektart, name, gemeinde_118, festnetz_118, fn_zentrale_118, mobile_118, mo_zentrale_118, poly_rgb_118 from bakom.notruf WHERE objektart = 'Sondergebiet' order by st_area(the_geom) DESC) AS bla USING UNIQUE bfs_nummer USING SRID=2056"
    DATA "the_geom from (SELECT the_geom, bfs_nummer, objektart, name, festnetz_143, fn_gemeinde_143, fn_addresse_143, fn_rgb_143 from bakom.notruf order by st_area(the_geom) DESC) AS bla USING UNIQUE bfs_nummer USING SRID=2056"
    DATA "the_geom from (SELECT the_geom, bfs_nummer, objektart, name, festnetz_143, fn_gemeinde_143, fn_addresse_143, fn_rgb_143 from bakom.notruf WHERE objektart = 'Sondergebiet' order by st_area(the_geom) DESC) AS bla USING UNIQUE bfs_nummer USING SRID=2056"
    DATA "the_geom from (SELECT the_geom, bfs_nummer, objektart, name, mobile_143, mo_gemeinde_143, mo_addresse_143, mo_rgb_143 from bakom.notruf order by st_area(the_geom) DESC) AS bla USING UNIQUE bfs_nummer USING SRID=2056"
    DATA "the_geom from (SELECT the_geom, bfs_nummer, objektart, name, mobile_143, mo_gemeinde_143, mo_addresse_143, mo_rgb_143 from bakom.notruf where objektart = 'Sondergebiet' order by st_area(the_geom) DESC) AS bla USING UNIQUE bfs_nummer USING SRID=2056"
    DATA "the_geom from (SELECT the_geom, bfs_nummer, objektart, name, gemeinde_143, festnetz_143, fn_zentrale_143, mobile_143, mo_zentrale_143, poly_rgb_143 from bakom.notruf order by st_area(the_geom) DESC) AS bla USING UNIQUE bfs_nummer USING SRID=2056"
    DATA "the_geom from (SELECT the_geom, bfs_nummer, objektart, name, gemeinde_143, festnetz_143, fn_zentrale_143, mobile_143, mo_zentrale_143, poly_rgb_143 from bakom.notruf WHERE objektart = 'Sondergebiet' order by st_area(the_geom) DESC) AS bla USING UNIQUE bfs_nummer USING SRID=2056"
    DATA "the_geom from (SELECT the_geom, bfs_nummer, objektart, name, festnetz_144, fn_gemeinde_144, fn_addresse_144, fn_rgb_144 from bakom.notruf order by st_area(the_geom) DESC) AS bla USING UNIQUE bfs_nummer USING SRID=2056"
    DATA "the_geom from (SELECT the_geom, bfs_nummer, objektart, name, festnetz_144, fn_gemeinde_144, fn_addresse_144, fn_rgb_144 from bakom.notruf WHERE objektart = 'Sondergebiet' order by st_area(the_geom) DESC) AS bla USING UNIQUE bfs_nummer USING SRID=2056"
    DATA "the_geom from (SELECT the_geom, bfs_nummer, objektart, name, mobile_144, mo_gemeinde_144, mo_addresse_144, mo_rgb_144 from bakom.notruf order by st_area(the_geom) DESC) AS bla USING UNIQUE bfs_nummer USING SRID=2056"
    DATA "the_geom from (SELECT the_geom, bfs_nummer, objektart, name, mobile_144, mo_gemeinde_144, mo_addresse_144, mo_rgb_144 from bakom.notruf where objektart = 'Sondergebiet' order by st_area(the_geom) DESC) AS bla USING UNIQUE bfs_nummer USING SRID=2056"
    DATA "the_geom from (SELECT the_geom, bfs_nummer, objektart, name, gemeinde_144, festnetz_144, fn_zentrale_144, mobile_144, mo_zentrale_144, poly_rgb_144 from bakom.notruf order by st_area(the_geom) DESC) AS bla USING UNIQUE bfs_nummer USING SRID=2056"
    DATA "the_geom from (SELECT the_geom, bfs_nummer, objektart, name, gemeinde_144, festnetz_144, fn_zentrale_144, mobile_144, mo_zentrale_144, poly_rgb_144 from bakom.notruf WHERE objektart = 'Sondergebiet' order by st_area(the_geom) DESC) AS bla USING UNIQUE bfs_nummer USING SRID=2056"
    DATA "the_geom from (SELECT the_geom, bfs_nummer, objektart, name, festnetz_145, fn_gemeinde_145, fn_addresse_145, fn_rgb_145 from bakom.notruf order by st_area(the_geom) DESC) AS bla USING UNIQUE bfs_nummer USING SRID=2056"
    DATA "the_geom from (SELECT the_geom, bfs_nummer, objektart, name, festnetz_145, fn_gemeinde_145, fn_addresse_145, fn_rgb_145 from bakom.notruf WHERE objektart = 'Sondergebiet' order by st_area(the_geom) DESC) AS bla USING UNIQUE bfs_nummer USING SRID=2056"
    DATA "the_geom from (SELECT the_geom, bfs_nummer, objektart, name, mobile_145, mo_gemeinde_145, mo_addresse_145, mo_rgb_145 from bakom.notruf order by st_area(the_geom) DESC) AS bla USING UNIQUE bfs_nummer USING SRID=2056"
    DATA "the_geom from (SELECT the_geom, bfs_nummer, objektart, name, mobile_145, mo_gemeinde_145, mo_addresse_145, mo_rgb_145 from bakom.notruf where objektart = 'Sondergebiet' order by st_area(the_geom) DESC) AS bla USING UNIQUE bfs_nummer USING SRID=2056"
    DATA "the_geom from (SELECT the_geom, bfs_nummer, objektart, name, gemeinde_145, festnetz_145, fn_zentrale_145, mobile_145, mo_zentrale_145, poly_rgb_145 from bakom.notruf order by st_area(the_geom) DESC) AS bla USING UNIQUE bfs_nummer USING SRID=2056"
    DATA "the_geom from (SELECT the_geom, bfs_nummer, objektart, name, gemeinde_145, festnetz_145, fn_zentrale_145, mobile_145, mo_zentrale_145, poly_rgb_145 from bakom.notruf WHERE objektart = 'Sondergebiet' order by st_area(the_geom) DESC) AS bla USING UNIQUE bfs_nummer USING SRID=2056"
    DATA "the_geom from (SELECT the_geom, bfs_nummer, objektart, name, festnetz_147, fn_gemeinde_147, fn_addresse_147, fn_rgb_147 from bakom.notruf order by st_area(the_geom) DESC) AS bla USING UNIQUE bfs_nummer USING SRID=2056"
    DATA "the_geom from (SELECT the_geom, bfs_nummer, objektart, name, festnetz_147, fn_gemeinde_147, fn_addresse_147, fn_rgb_147 from bakom.notruf WHERE objektart = 'Sondergebiet' order by st_area(the_geom) DESC) AS bla USING UNIQUE bfs_nummer USING SRID=2056"
    DATA "the_geom from (SELECT the_geom, bfs_nummer, objektart, name, mobile_147, mo_gemeinde_147, mo_addresse_147, mo_rgb_147 from bakom.notruf order by st_area(the_geom) DESC) AS bla USING UNIQUE bfs_nummer USING SRID=2056"
    DATA "the_geom from (SELECT the_geom, bfs_nummer, objektart, name, mobile_147, mo_gemeinde_147, mo_addresse_147, mo_rgb_147 from bakom.notruf where objektart = 'Sondergebiet' order by st_area(the_geom) DESC) AS bla USING UNIQUE bfs_nummer USING SRID=2056"
    DATA "the_geom from (SELECT the_geom, bfs_nummer, objektart, name, gemeinde_147, festnetz_147, fn_zentrale_147, mobile_147, mo_zentrale_147, poly_rgb_147 from bakom.notruf order by st_area(the_geom) DESC) AS bla USING UNIQUE bfs_nummer USING SRID=2056"
    DATA "the_geom from (SELECT the_geom, bfs_nummer, objektart, name, gemeinde_147, festnetz_147, fn_zentrale_147, mobile_147, mo_zentrale_147, poly_rgb_147 from bakom.notruf WHERE objektart = 'Sondergebiet' order by st_area(the_geom) DESC) AS bla USING UNIQUE bfs_nummer USING SRID=2056"
    DATA "the_geom from (SELECT * from bakom.notruf order by objektart asc) AS bla USING UNIQUE bfs_nummer USING SRID=2056"
    DATA "the_geom from (SELECT * from bakom.richtfunkverbindungen ORDER BY link_class ASC) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from  (SELECT * from bakom.tv_gebiet_overlap) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from  (SELECT *  from bakom.ukw_gebiet_overlap) as data using unique bgdi_id using srid=2056"
    DATA "the_geom FROM (SELECT bgdi_id, the_geom, bgdi_buffer_area, name_fr, bgdi_name as name_de, name_it, name_en, name_rm, type_fr, type_de, type_it, type_en, type_rm, bgdi_surface FROM unesco ) as blabla USING UNIQUE bgdi_id USING SRID=2056"
    DATA "the_geom FROM (select bgdi_buffer_area, bgdi_id, the_geom , name_fr, bgdi_name as name_de, name_it, name_en, name_rm, type_fr, type_de, type_it, type_en, type_rm, bgdi_surface from unesco WHERE (st_area(the_geom) > 600000 AND bgdi_buffer_area = 'N') OR (st_area(the_geom) > 5000000 AND bgdi_buffer_area = 'Y')) as blabla USING UNIQUE bgdi_id USING SRID=2056"
    DATA "the_geom FROM (select bgdi_buffer_area, st_area(the_geom) as area_surface, bgdi_id, st_centroid(the_geom) as the_geom , name_fr, bgdi_name as name_de, name_it, name_en, name_rm, type_fr, type_de, type_it, type_en, type_rm, bgdi_surface from unesco WHERE bgdi_buffer_area = 'N' AND st_area(the_geom) < 5000000 and not st_contains((select st_setsrid(st_union(st_buffer(box2d(the_geom),3000)),2056) from unesco where st_area(the_geom) > 5000000),st_setsrid(st_centroid(the_geom),2056))) as blabla USING UNIQUE bgdi_id USING SRID=2056"
    DATA "the_geom from (SELECT bgdi_id, nasak_nr, kategorie_de, art_der_anlage, name_der_anlage, ort, website, symboltyp, the_geom from baspo.nationales_sportanlagenkonzept) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT bgdi_id, dst_nr, name, dst_abk, isb, typ, typ_de, typ_fr, typ_it, the_geom from bav.anlagen_schienengueterverkehr) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT  bgdi_id, the_geom, stabil_id, text_aussage_%lang% as text_aussage, text_thema_%lang% as text_thema, text_rechtsstatus_%lang% as text_rechtsstatus, publiziertab, publiziertbis, text_amt_%lang% as text_amt from bav.baulinien_eisenbahnanlagen_v2_0_oereb) as data using unique stabil_id using srid=2056"
    DATA "the_geom from (select * from bav.stoerfallverordnung_eisenbahnanlagen_betriebe) as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * from bav.oev_haltekante) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * from bav.oev_haltestellen) as data using unique bgdi_id using srid=2056"
    DATA "the_geom FROM (
    DATA "the_geom from (select * FROM bav.kataster_belasteter_standorte_oev ORDER BY statusaltlv_nummer DESC,st_area(the_geom) DESC) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (select  bgdi_id, the_geom, stabil_id, bgdi_class, class_weight, text_aussage_%lang% as text_aussage, text_thema_%lang% as text_thema, text_rechtsstatus_%lang% as text_rechtsstatus, publiziertab, text_amt_%lang% as text_amt FROM bav.kataster_belasteter_standorte_oev_oereb ORDER BY class_weight DESC,st_area(the_geom) DESC) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (select  bgdi_id, the_geom, stabil_id, bgdi_class, class_weight, text_aussage_%lang% as text_aussage, text_thema_%lang% as text_thema, text_rechtsstatus_%lang% as text_rechtsstatus, publiziertab, text_amt_%lang% as text_amt FROM bav.kataster_belasteter_standorte_oev_oereb ORDER BY class_weight DESC,st_area(the_geom) DESC) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (select  bgdi_id, the_geom, stabil_id, bgdi_class, class_weight, text_aussage_%lang% as text_aussage, text_thema_%lang% as text_thema, text_rechtsstatus_%lang% as text_rechtsstatus, publiziertab, publiziertbis, text_amt_%lang% as text_amt FROM bav.kataster_belasteter_standorte_oev_v2_0_oereb ORDER BY class_weight DESC,st_area(the_geom) DESC) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (select  bgdi_id, the_geom, stabil_id, bgdi_class, class_weight, text_aussage_%lang% as text_aussage, text_thema_%lang% as text_thema, text_rechtsstatus_%lang% as text_rechtsstatus, publiziertab, publiziertbis, text_amt_%lang% as text_amt FROM bav.kataster_belasteter_standorte_oev_v2_0_oereb ORDER BY class_weight DESC,st_area(the_geom) DESC) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (
    DATA "the_geom from (
    DATA "the_geom from (SELECT *  from bav.laerm_eisenbahn_festgelegte_emissionen_nacht) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT *  from bav.laerm_eisenbahn_festgelegte_emissionen_tag) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (
    DATA "the_geom from (SELECT *  from bav.laerm_eisenbahn_tatsaechliche_emissionen_nacht) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT *  from bav.laerm_eisenbahn_tatsaechliche_emissionen_tag) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (
    DATA "the_geom from (
    DATA "the_geom from (SELECT id, lin_nr_dfa, the_geom from diverse.laerm_emplan_bahn_2015) as data using unique id using srid=2056"
    DATA "the_geom from (SELECT * from bav.stoerfallverordnung_eisenbahnanlagen_lage WHERE geom_type LIKE '%polygon%') as blabla  using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * from bav.stoerfallverordnung_eisenbahnanlagen_lage WHERE geom_type LIKE '%line%') as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT  bgdi_id, the_geom, stabil_id, text_aussage_%lang% as text_aussage, text_thema_%lang% as text_thema, text_rechtsstatus_%lang% as text_rechtsstatus, publiziertab, publiziertbis, text_amt_%lang% as text_amt from bav.projektierungszonen_eisenbahnanlagen_v2_0_oereb) as data using unique stabil_id using srid=2056"
    DATA "the_geom from  (SELECT *  from bav.sis_pl_anhorung WHERE geom_type like '%area%' order by coordlevel_tid desc) as data using unique stabil_id using srid=2056"
    DATA "the_geom from  (SELECT *  from bav.sis_pl_r_anhorung WHERE geom_type like '%area%' order by coordlevel_tid desc) as data using unique stabil_id using srid=2056"
    DATA "the_geom from  (SELECT *  from bav.sis_pl_anhorung WHERE geom_type like '%area%' order by coordlevel_tid desc) as data using unique stabil_id using srid=2056"
    DATA "the_geom from  (SELECT *  from bav.sis_pl_r_anhorung WHERE geom_type like '%area%' order by coordlevel_tid desc) as data using unique stabil_id using srid=2056"
    DATA "the_geom from  (SELECT *  from bav.sis_fac_anhorung WHERE geom_type like '%line%') as data using unique stabil_id using srid=2056"
    DATA "the_geom from  (SELECT *  from bav.sis_fac_anhorung WHERE geom_type like '%line%') as data using unique stabil_id using srid=2056"
    DATA "the_geom from  (SELECT *  from bav.sis_fac_anhorung WHERE geom_type like '%point%') as data using unique stabil_id using srid=2056"
    DATA "the_geom from  (SELECT *  from bav.sis_fac_anhorung WHERE geom_type like '%point%') as data using unique stabil_id using srid=2056"
    DATA "the_geom from (SELECT * from bav.sis_angaben WHERE the_geom_type = 'line') as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * from bav.sis_angaben WHERE the_geom_type = 'line') as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT *, (360-symbolori) AS angle from bav.sis_angaben WHERE the_geom_type = 'point') as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT *, (360-symbolori) AS angle from bav.sis_angaben WHERE the_geom_type = 'point') as data using unique bgdi_id using srid=2056"
    DATA "the_geom from  (SELECT stabil_id,facname_de,facname_fr,facname_it,plname_de,plname_fr,plname_it,meastype_text_de,meastype_text_fr,meastype_text_it,coordlevel_text_de,coordlevel_text_fr,coordlevel_text_it,plstatus_text_de,plstatus_text_fr,plstatus_text_it,validfrom,validuntil,description_de,description_fr,description_it,doc_web,doc_title,bgdi_created,meastype_tid, coordlevel_tid, st_centroid(the_geom) as the_geom from bav.sis_pl_kraft WHERE geom_type like '%area%' order by coordlevel_tid desc) as data using unique stabil_id using srid=2056"
    DATA "the_geom from  (SELECT *  from bav.sis_pl_kraft WHERE geom_type like '%area%' order by coordlevel_tid desc) as data using unique stabil_id using srid=2056"
    DATA "the_geom from  (SELECT *  from bav.sis_pl_r_kraft WHERE geom_type like '%area%' order by coordlevel_tid desc) as data using unique stabil_id using srid=2056"
    DATA "the_geom from  (SELECT *  from bav.sis_pl_kraft WHERE geom_type like '%area%' order by coordlevel_tid desc) as data using unique stabil_id using srid=2056"
    DATA "the_geom from  (SELECT * from bav.sis_pl_r_kraft WHERE geom_type like '%area%' order by coordlevel_tid desc) as data using unique stabil_id using srid=2056"
    DATA "the_geom from  (SELECT * from bav.sis_fac_kraft WHERE geom_type like '%line%') as data using unique stabil_id using srid=2056"
    DATA "the_geom from  (SELECT * from bav.sis_fac_kraft WHERE geom_type like '%line%') as data using unique stabil_id using srid=2056"
    DATA "the_geom from  (SELECT *  from bav.sis_fac_kraft WHERE geom_type like '%point%') as data using unique stabil_id using srid=2056"
    DATA "the_geom from  (SELECT *  from bav.sis_fac_kraft WHERE geom_type like '%point%') as data using unique stabil_id using srid=2056"
    DATA "the_geom from (SELECT *  from bav.sif_pl_anhorung WHERE geom_type like '%area%') as data using unique stabil_id using srid=2056"
    DATA "the_geom from (SELECT *  from bav.sif_pl_r_anhorung) as data using unique stabil_id using srid=2056"
    DATA "the_geom from (SELECT *  from bav.sif_fac_anhorung WHERE geom_type like '%line%') as data using unique stabil_id using srid=2056"
    DATA "the_geom from (SELECT *  from bav.sif_fac_anhorung WHERE geom_type like '%line%') as data using unique stabil_id using srid=2056"
    DATA "the_geom from (SELECT *  from bav.sif_fac_anhorung WHERE geom_type like '%line%') as data using unique stabil_id using srid=2056"
    DATA "the_geom from (SELECT *  from bav.sif_fac_anhorung WHERE geom_type like '%line%') as data using unique stabil_id using srid=2056"
    DATA "the_geom from (SELECT *, (360-symbolori) AS origin from bav.sif_ausgangslage) as data using unique anlage_id using srid=2056"
    DATA "the_geom from (SELECT *  from bav.sif_pl_kraft WHERE geom_type like '%area%') as data using unique stabil_id using srid=2056"
    DATA "the_geom from (SELECT *  from bav.sif_pl_r_kraft) as data using unique stabil_id using srid=2056"
    DATA "the_geom from (SELECT *  from bav.sif_fac_kraft WHERE geom_type like '%line%') as data using unique stabil_id using srid=2056"
    DATA "the_geom from (SELECT *  from bav.sif_fac_kraft WHERE geom_type like '%line%') as data using unique stabil_id using srid=2056"
    DATA "the_geom from (SELECT *  from bav.sif_fac_kraft WHERE geom_type like '%line%') as data using unique stabil_id using srid=2056"
    DATA "the_geom from (SELECT *  from bav.sif_fac_kraft WHERE geom_type like '%line%') as data using unique stabil_id using srid=2056"
    DATA "the_geom from (SELECT xtf_id, the_geom, meastype_tid, coordlevel_tid from bav.sug_pl_anhorung) as data using unique xtf_id using srid=2056"
    DATA "the_geom from (SELECT xtf_id, the_geom, meastype_tid, coordlevel_tid from bav.sug_pl_anhorung) as data using unique xtf_id using srid=2056"
    DATA "the_geom from (SELECT xtf_id, the_geom, fackind_tid, facstatus_tid from bav.sug_fac_anhorung) as data using unique xtf_id using srid=2056"
    DATA "the_geom from (SELECT xtf_id, the_geom, fackind_tid, facstatus_tid from bav.sug_fac_anhorung) as data using unique xtf_id using srid=2056"
    DATA "the_geom from (SELECT xtf_id, the_geom, meastype_tid, coordlevel_tid from bav.sug_pl_kraft) as data using unique xtf_id using srid=2056"
    DATA "the_geom from (SELECT xtf_id, the_geom, meastype_tid, coordlevel_tid from bav.sug_pl_kraft) as data using unique xtf_id using srid=2056"
    DATA "the_geom from (SELECT xtf_id, the_geom, fackind_tid, facstatus_tid from bav.sug_fac_kraft) as data using unique xtf_id using srid=2056"
    DATA "the_geom from (SELECT xtf_id, the_geom, fackind_tid, facstatus_tid from bav.sug_fac_kraft) as data using unique xtf_id using srid=2056"
    DATA "the_geom from (SELECT *  from bav.schienennetz_segment) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT *  from bav.schienennetz_point) as data using unique xtf_id using srid=2056"
    DATA "the_geom FROM bav.seilbahnen_bundeskonzession_seilbahnstrecke using unique bgdi_id using srid=2056"
    DATA "the_geom FROM bav.seilbahnen_bundeskonzession_station using unique bgdi_id using srid=2056"
    DATA "the_geom FROM (SELECT * FROM bav.seilbahnen_bundeskonzession_seilstuetzen) AS blabla using unique bgdi_id using srid=2056"
    DATA "the_geom FROM bav.seilbahnen_bundeskonzession_bauwerk using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT  bgdi_id, the_geom, stabil_id, text_aussage_%lang% as text_aussage, text_thema_%lang% as text_thema, text_rechtsstatus_%lang% as text_rechtsstatus, publiziertab, publiziertbis, text_amt_%lang% as text_amt from bazl.baulinien_flughafenanlagen_v2_0_oereb) as data using unique stabil_id using srid=2056"
    DATA "the_geom from (SELECT * from bazl.bebaute_gebiete WHERE bebaut) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * from bazl.gebietsbeschraenkungen_drohnen) as bla using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * from bazl.gebietsbeschraenkungen_drohnen) as bla using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * from bazl.gebietsbeschraenkungen_drohnen) as bla using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * from bazl.gebietsbeschraenkungen_drohnen) as bla using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT ident, bgdi_id, the_geom, arp_east, arp_north, canton, elevation, icao, location, name, sym_code, CASE WHEN sym_code in (11,12,13,14,15,16,17) THEN -sym_ori + 45 ELSE sym_ori END AS sym_ori from bazl.flugplaetze_heliports) as geometrie using unique bgdi_id using srid=2056"
    DATA "the_geom FROM (SELECT * FROM bazl.gebirgslandeplaetze) AS blabla using unique bgdi_id using srid=2056"
    DATA "the_geom FROM (SELECT * FROM bazl.hindernisbegrenzungsflaechen_kataster_olsline) AS bla USING UNIQUE bgdi_id USING SRID=2056"
    DATA "the_geom FROM (SELECT * FROM bazl.hindernisbegrenzungsflaechen_kataster_area) AS bla USING UNIQUE bgdi_id USING SRID=2056"
    DATA "the_geom from (SELECT * from bazl.hindernisbegrenzungsflaechen_perimeter ORDER BY status DESC) AS bla USING UNIQUE bgdi_id USING SRID=2056"
    DATA "the_geom FROM (SELECT id, the_geom, density_pop_km2 FROM bazl.intrinsisches_bodenrisiko_sora) AS geometrie USING UNIQUE id USING srid=2056"
    DATA "the_geom FROM (SELECT * FROM bazl.kataster_belasteter_standorte_zivflpl  WHERE geom_type LIKE '%area%' ORDER BY class_weight DESC,st_area(the_geom) DESC) AS blabla using unique vflz_id using srid=2056"
    DATA "the_geom from (select * FROM bazl.kataster_belasteter_standorte_zivflpl WHERE geom_type LIKE '%area%' ORDER BY class_weight DESC) as data using unique bgdi_id using srid=2056"
    DATA "the_geom FROM (SELECT * FROM bazl.kataster_belasteter_standorte_zivflpl WHERE geom_type LIKE '%point%' ORDER BY class_weight DESC) AS blabla using unique vflz_id using srid=2056"
    DATA "the_geom from (select bgdi_id, the_geom, stabil_id, bgdi_class, class_weight, text_aussage_%lang% as text_aussage, text_thema_%lang% as text_thema, text_rechtsstatus_%lang% as text_rechtsstatus, publiziertab, text_amt_%lang% as text_amt FROM bazl.kataster_belasteter_standorte_zivflpl_oereb WHERE geom_type LIKE '%area%' ORDER BY class_weight DESC,st_area(the_geom) DESC) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (select  bgdi_id, the_geom, stabil_id, bgdi_class, class_weight, text_aussage_%lang% as text_aussage, text_thema_%lang% as text_thema, text_rechtsstatus_%lang% as text_rechtsstatus, publiziertab, text_amt_%lang% as text_amt FROM bazl.kataster_belasteter_standorte_zivflpl_oereb WHERE geom_type LIKE '%area%' ORDER BY class_weight DESC) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (select bgdi_id, the_geom, stabil_id, bgdi_class, class_weight, text_aussage_%lang% as text_aussage, text_thema_%lang% as text_thema, text_rechtsstatus_%lang% as text_rechtsstatus, publiziertab, text_amt_%lang% as text_amt FROM bazl.kataster_belasteter_standorte_zivflpl_oereb WHERE geom_type LIKE '%point%' ORDER BY class_weight DESC) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (select bgdi_id, the_geom, stabil_id, bgdi_class, class_weight, text_aussage_%lang% as text_aussage, text_thema_%lang% as text_thema, text_rechtsstatus_%lang% as text_rechtsstatus, publiziertab, publiziertbis, text_amt_%lang% as text_amt FROM bazl.kataster_belasteter_standorte_zivflpl_v2_0_oereb WHERE geom_type LIKE '%area%' ORDER BY class_weight DESC,st_area(the_geom) DESC) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (select  bgdi_id, the_geom, stabil_id, bgdi_class, class_weight, text_aussage_%lang% as text_aussage, text_thema_%lang% as text_thema, text_rechtsstatus_%lang% as text_rechtsstatus, publiziertab, publiziertbis, text_amt_%lang% as text_amt FROM bazl.kataster_belasteter_standorte_zivflpl_v2_0_oereb WHERE geom_type LIKE '%area%' ORDER BY class_weight DESC) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (select bgdi_id, the_geom, stabil_id, bgdi_class, class_weight, text_aussage_%lang% as text_aussage, text_thema_%lang% as text_thema, text_rechtsstatus_%lang% as text_rechtsstatus, publiziertab, publiziertbis, text_amt_%lang% as text_amt FROM bazl.kataster_belasteter_standorte_zivflpl_v2_0_oereb WHERE geom_type LIKE '%point%' ORDER BY class_weight DESC) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from  (SELECT *,mod(exposurecurve_level_db::integer, 5) as bgdi_modulo_5 FROM bazl.laermbelastungskataster_zivilflugplaetze ) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from  (SELECT *,mod(exposurecurve_level_db::integer, 5) as bgdi_modulo_5 FROM bazl.laermbelastungskataster_zivilflugplaetze ) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from  (SELECT *,mod(exposurecurve_level_db::integer, 5) as bgdi_modulo_5 FROM bazl.laermbelastungskataster_zivilflugplaetze ) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from  (SELECT *,mod(exposurecurve_level_db::integer, 5) as bgdi_modulo_5 FROM bazl.laermbelastungskataster_zivilflugplaetze ) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from  (SELECT *,mod(exposurecurve_level_db::integer, 5) as bgdi_modulo_5 FROM bazl.laermbelastungskataster_zivilflugplaetze ) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from  (SELECT *,mod(exposurecurve_level_db::integer, 5) as bgdi_modulo_5 FROM bazl.laermbelastungskataster_zivilflugplaetze ) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from  (SELECT *,mod(exposurecurve_level_db::integer, 5) as bgdi_modulo_5 FROM bazl.laermbelastungskataster_zivilflugplaetze ) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from  (SELECT *,mod(exposurecurve_level_db::integer, 5) as bgdi_modulo_5 FROM bazl.laermbelastungskataster_zivilflugplaetze ) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from bazl.landschaftsruhezonen using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT registrationnumber, airport, obstacletype, maxheightagl, topelevationamsl, radius, effectivedate, modified, marking, lighting, \"group\", uuid, ogr_style, the_geom, bgdi_geom from bazl.view_obstacles_active) AS bla USING UNIQUE registrationnumber USING SRID=2056"
    DATA "the_geom from (SELECT registrationnumber, airport, obstacletype, maxheightagl, topelevationamsl, radius, effectivedate, modified, marking, lighting, \"group\", uuid, ogr_style, the_geom, bgdi_geom from bazl.view_obstacles_active) AS bla USING UNIQUE registrationnumber USING SRID=2056"
    DATA "the_geom from (SELECT registrationnumber, airport, obstacletype, maxheightagl, topelevationamsl, radius, effectivedate, modified, marking, lighting, \"group\", uuid, ogr_style, the_geom, bgdi_geom from bazl.view_obstacles_active) AS bla USING UNIQUE registrationnumber USING SRID=2056"
    DATA "the_geom from (SELECT bgdi_id as id, st_GeometryFromText( 'POINT ( 0 0 )', 2056) as the_geom, 'Last update: ' || to_char(bgdi_created,'dd.mm.yyyy') stand from bazl.view_obstacles_active limit 1) AS bla USING UNIQUE id USING SRID=2056"
    DATA "the_geom from (SELECT registrationnumber, airport, obstacletype, maxheightagl, topelevationamsl, radius, effectivedate, modified, marking, lighting, \"group\", uuid, ogr_style, the_geom, bgdi_geom from bazl.view_obstacles_small) AS bla USING UNIQUE registrationnumber USING SRID=2056"
    DATA "the_geom from (SELECT registrationnumber, airport, obstacletype, maxheightagl, topelevationamsl, radius, effectivedate, modified, marking, lighting, \"group\", uuid, ogr_style, the_geom, bgdi_geom from bazl.view_obstacles_small) AS bla USING UNIQUE registrationnumber USING SRID=2056"
    DATA "the_geom from (SELECT registrationnumber, airport, obstacletype, maxheightagl, topelevationamsl, radius, effectivedate, modified, marking, lighting, \"group\", uuid, ogr_style, the_geom, bgdi_geom from bazl.view_obstacles_small) AS bla USING UNIQUE registrationnumber USING SRID=2056"
    DATA "the_geom from (SELECT bgdi_id as id, st_GeometryFromText( 'POINT ( 0 0 )', 2056) as the_geom, 'Last update: ' || to_char(bgdi_created,'dd.mm.yyyy') stand from bazl.view_obstacles_small limit 1) AS bla USING UNIQUE id USING SRID=2056"
    DATA "the_geom from (SELECT registrationnumber, airport, obstacletype, maxheightagl, topelevationamsl, radius, effectivedate, modified, marking, lighting, \"group\", uuid, ogr_style, the_geom, bgdi_geom from bazl.view_obstacles_big) AS bla USING UNIQUE registrationnumber USING SRID=2056"
    DATA "the_geom from (SELECT registrationnumber, airport, obstacletype, maxheightagl, topelevationamsl, radius, effectivedate, modified, marking, lighting, \"group\", uuid, ogr_style, the_geom, bgdi_geom from bazl.view_obstacles_big) AS bla USING UNIQUE registrationnumber USING SRID=2056"
    DATA "the_geom from (SELECT registrationnumber, airport, obstacletype, maxheightagl, topelevationamsl, radius, effectivedate, modified, marking, lighting, \"group\", uuid, ogr_style, the_geom, bgdi_geom from bazl.view_obstacles_big) AS bla USING UNIQUE registrationnumber USING SRID=2056"
    DATA "the_geom from (SELECT bgdi_id as id, st_GeometryFromText( 'POINT ( 0 0 )', 2056) as the_geom, 'Last update: ' || to_char(bgdi_created,'dd.mm.yyyy') stand from bazl.view_obstacles_big limit 1) AS bla USING UNIQUE id USING SRID=2056"
    DATA "the_geom from (SELECT the_geom, name, designator, type, loli_value, loli_type, upli_value, upli_type, bgdi_id from bazl.luftraeume_fluginformationsgebiet) AS bla USING UNIQUE bgdi_id USING SRID=2056"
    DATA "the_geom from (SELECT the_geom, name, designator, type, loli_value, loli_type, upli_value, upli_type, bgdi_id from bazl.luftraeume_fluginformationszonen) AS bla USING UNIQUE bgdi_id USING SRID=2056"
    DATA "the_geom from (SELECT the_geom, name, designator, type, loli_value, loli_type, upli_value, upli_type, bgdi_id from bazl.luftraeume_kontrollbezirke) AS bla USING UNIQUE bgdi_id USING SRID=2056"
    DATA "the_geom from (SELECT the_geom, type, bgdi_id, designator, loli_type, loli_value, upli_type, upli_value from bazl.luftraeume_kontrollzonen) AS bla USING UNIQUE bgdi_id USING SRID=2056"
    DATA "the_geom from (SELECT the_geom, type, bgdi_id, designator, loli_type, loli_value, upli_type, upli_value from bazl.luftraeume_nahkontrollbezirke) AS bla USING UNIQUE bgdi_id USING SRID=2056"
    DATA "the_geom from (SELECT bgdi_id, the_geom, stabil_id, text_aussage_%lang% as text_aussage, text_thema_%lang% as text_thema, text_rechtsstatus_%lang% as text_rechtsstatus, publiziertab, publiziertbis, text_amt_%lang% as text_amt from bazl.projektierungszonen_v2_0_oereb WHERE rechtsstatus IN ('AenderungOhneVorwirkung', 'AenderungMitVorwirkung')) as data using unique stabil_id using srid=2056"
    DATA "the_geom from (SELECT * from bazl.projektierungszonen) as data using unique stabil_id using srid=2056"
    DATA "the_geom from (SELECT  bgdi_id, the_geom, stabil_id, text_aussage_%lang% as text_aussage, text_thema_%lang% as text_thema, text_rechtsstatus_%lang% as text_rechtsstatus, publiziertab, publiziertbis, text_amt_%lang% as text_amt from bazl.projektierungszonen_v2_0_oereb) as data using unique stabil_id using srid=2056"
    DATA "the_geom from  (SELECT * from bazl.sachplan_inf_luft_plmeasures_r_anhorung WHERE geom_type like '%area%' ORDER BY coordid desc) as data using unique stabil_id using srid=2056"
    DATA "the_geom from  (SELECT * from bazl.sachplan_inf_luft_facilities_r_anhorung WHERE geom_type like '%area%' ORDER BY coordid desc) as data using unique stabil_id using srid=2056"
    DATA "the_geom from (SELECT * from bazl.sachplan_inf_luft_facilities_anhorung WHERE geom_type like '%area%' ORDER BY coordid desc) as data using unique stabil_id using srid=2056"
    DATA "the_geom from  (SELECT *  from bazl.sachplan_inf_luft_plmeasures_anhorung WHERE geom_type like '%area%' ORDER BY coordid desc) as data using unique stabil_id using srid=2056"
    DATA "the_geom from (SELECT * from bazl.sachplan_inf_luft_plmeasures_anhorung WHERE geom_type like '%point%' ORDER BY ordering asc) as data using unique stabil_id using srid=2056"
    DATA "the_geom from (SELECT * from bazl.sachplan_inf_luft_plmeasures_anhorung WHERE geom_type like '%point%' ORDER BY ordering asc) as data using unique stabil_id using srid=2056"
    DATA "the_geom from (SELECT * from bazl.sachplan_inf_luft_facilities_anhorung WHERE geom_type like '%point%' ORDER BY ordering asc) as data using unique stabil_id using srid=2056"
    DATA "the_geom from  (SELECT *  from bazl.sachplan_inf_luft_plmeasures_r_kraft WHERE geom_type like '%area%' ORDER BY coordid desc) as data using unique stabil_id using srid=2056"
    DATA "the_geom from  (SELECT *  from bazl.sachplan_inf_luft_facilities_r_kraft WHERE geom_type like '%area%' ORDER BY coordid desc) as data using unique stabil_id using srid=2056"
    DATA "the_geom from  (SELECT *  from bazl.sachplan_inf_luft_facilities_kraft WHERE geom_type like '%area%' ORDER BY coordid desc) as data using unique stabil_id using srid=2056"
    DATA "the_geom from  (SELECT *  from bazl.sachplan_inf_luft_plmeasures_kraft WHERE geom_type like '%area%' ORDER BY coordid desc) as data using unique stabil_id using srid=2056"
    DATA "the_geom from  (SELECT *  from bazl.sachplan_inf_luft_plmeasures_kraft WHERE geom_type like '%point%' ORDER BY ordering asc) as data using unique stabil_id using srid=2056"
    DATA "the_geom from  (SELECT *  from bazl.sachplan_inf_luft_plmeasures_kraft WHERE geom_type like '%point%' ORDER BY ordering asc) as data using unique stabil_id using srid=2056"
    DATA "the_geom from  (SELECT *  from bazl.sachplan_inf_luft_facilities_kraft WHERE geom_type like '%point%' ORDER BY ordering asc) as data using unique stabil_id using srid=2056"
    DATA "the_geom from (SELECT bgdi_id, schutzg_id, the_geom from bazl.schutzgebiete_aulav_liechtenstein) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT  bgdi_id, the_geom, stabil_id, text_aussage_%lang% as text_aussage, text_thema_%lang% as text_thema, text_rechtsstatus_%lang% as text_rechtsstatus, publiziertab, publiziertbis, text_amt_%lang% as text_amt from bazl.sichereitszonen_v2_0_oereb WHERE rechtsstatus IN ('AenderungOhneVorwirkung', 'AenderungMitVorwirkung')) as data using unique stabil_id using srid=2056"
    DATA "the_geom from (SELECT stabil_id, zone, zonetype_tid, type_id, zonetype_de, zonetype_fr, zonetype_it, zone_name, originator, canton, municipality, approval_date, status_id, legalstatus_tid, legalstatus_de, legalstatus_fr, legalstatus_it, title, weblink, doc_description, doc_id, the_geom, CASE WHEN zonetype_tid LIKE '108-T-04' THEN 3 WHEN zonetype_tid LIKE '108-T-05' THEN 2 WHEN zonetype_tid LIKE '108-T-02' THEN 1 WHEN zonetype_tid LIKE '108-T-03' THEN 0 ELSE 0 END AS paint_order from bazl.sichereitszonen ORDER BY paint_order ASC) as data using unique stabil_id using srid=2056"
    DATA "the_geom from (SELECT  bgdi_id, the_geom, stabil_id, text_aussage_%lang% as text_aussage, text_thema_%lang% as text_thema, text_rechtsstatus_%lang% as text_rechtsstatus, publiziertab, publiziertbis, text_amt_%lang% as text_amt from bazl.sichereitszonen_v2_0_oereb WHERE rechtsstatus LIKE 'inKraft') as data using unique stabil_id using srid=2056"
    DATA "the_geom from (SELECT bgdi_id, the_geom, sym_code, arp_east, arp_north, canton, location, name from bazl.spitallandeplaetze) as geometrie using unique bgdi_id using srid=2056"
    DATA "the_geom FROM (SELECT the_geom, objectnumber, name, area, perimeter, startprotectioncommitment, endprotectioncommitment, modinfo FROM bfe.abgeltung_wasserkraftnutzung) AS blabla using unique objectnumber using srid=2056"
    DATA "the_geom from (SELECT  bgdi_id, the_geom, stabil_id, text_aussage_%lang% as text_aussage, text_thema_%lang% as text_thema, text_rechtsstatus_%lang% as text_rechtsstatus, publiziertab, publiziertbis, text_amt_%lang% as text_amt from bfe.baulinien_starkstromanlagen_v2_0_oereb WHERE rechtsstatus IN ('AenderungOhneVorwirkung', 'AenderungMitVorwirkung')) as data using unique stabil_id using srid=2056"
    DATA "the_geom from (SELECT  bgdi_id, the_geom, stabil_id, text_aussage_%lang% as text_aussage, text_thema_%lang% as text_thema, text_rechtsstatus_%lang% as text_rechtsstatus, publiziertab, publiziertbis, text_amt_%lang% as text_amt from bfe.baulinien_starkstromanlagen_v2_0_oereb WHERE rechtsstatus LIKE 'inKraft') as data using unique stabil_id using srid=2056"
    DATA "the_geom from (SELECT * from bfe.biogasanlagen) AS bla  USING UNIQUE bgdi_id USING SRID=2056"
    DATA "the_geom from (SELECT bgdi_id, nw_code, the_geom, name, non_woody, bfs_nummer from bfe.biomasse) as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT bgdi_id,  w_code, the_geom, name, woody, bfs_nummer from bfe.biomasse) as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * from bfe.elektrische_anlagen_ueber_36_station_poly) as blabla using unique id using srid=2056"
    DATA "the_geom from (SELECT * from bfe.elektrische_anlagen_ueber_36_station_point_no_poly) as blabla using unique id using srid=2056"
    DATA "the_geom from (SELECT * from bfe.elektrische_anlagen_ueber_36_station_point_with_poly) as blabla using unique id using srid=2056"
    DATA "the_geom from (SELECT * from bfe.elektrische_anlagen_ueber_36_line) as blabla using unique id using srid=2056"
    DATA "the_geom from (SELECT * from bfe.elektrische_anlagen_ueber_36_point) as blabla using unique id using srid=2056"
    DATA "the_geom FROM (
    DATA "the_geom from (
    DATA "the_geom FROM (SELECT * FROM bfe.energieforschung_projekte) AS data using unique bgdi_id using srid=2056"
    DATA "the_geom FROM (SELECT * FROM bfe.energiestaedte_2000watt_areale ORDER BY kategorie ASC) AS blabla using unique bgdi_id using srid=2056"
    DATA "the_geom FROM (SELECT * FROM bfe.energiestaedte_2000watt_areale ORDER BY kategorie ASC limit 0) AS blabla using unique bgdi_id using srid=2056"
    DATA "the_geom FROM (SELECT * FROM bfe.energiestaedte ORDER BY kategorie ASC) AS blabla using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * from bfe.renewable_heating_efh) AS bla  USING UNIQUE bgdi_id USING SRID=2056"
    DATA "the_geom from (SELECT * from bfe.renewable_heating_mfh) AS bla  USING UNIQUE bgdi_id USING SRID=2056"
    DATA "the_geom FROM (SELECT * FROM bfe.fernwaerme_angebot) AS blabla USING UNIQUE bgdi_id USING srid=2056"
    DATA "the_geom from (SELECT * from bfe.fernwaerme_industrie) AS bla USING UNIQUE bgdi_id USING SRID=2056"
    DATA "the_geom FROM bfe.fernwaerme_wohn USING UNIQUE bgdi_id USING SRID=2056"
    DATA "the_geom from (SELECT * from bfe.kehrichtverbrennungsanlagen) AS bla  USING UNIQUE bgdi_id USING SRID=2056"
    DATA "the_geom FROM (SELECT * FROM bfe.kernkraftwerke) as blabla using unique stabil_id using srid=2056"
    DATA "the_geom FROM (SELECT * FROM bfe.kleinwasserkraftpotentiale ORDER BY kwprometer ASC) AS data using unique bgdi_id using srid=2056"
    DATA "the_geom FROM bfe.komo_projekte USING UNIQUE bgdi_id USING SRID=2056"
    DATA "the_geom from (SELECT * from public.v_evse_aggregated_wms) AS bla  USING UNIQUE location_id USING SRID=2056"
    DATA "the_geom from (SELECT *, vereisung as frequency, hoehe as height from bfe.meteorologische_vereisung) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT bgdi_id, certificate as zertifikat, standard, canton as kanton, buildinginfo_de as gebaeude, buildinginfo_fr, buildinginfo_it, buildinginfo_en, ebf as energiebezugsflaeche, http_de as weitere_informationen, http_fr, http_it, http_en, the_geom from bfe.minergiegebaeude) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT  bgdi_id, the_geom, stabil_id, text_aussage_%lang% as text_aussage, text_thema_%lang% as text_thema, text_rechtsstatus_%lang% as text_rechtsstatus, publiziertab, publiziertbis, text_amt_%lang% as text_amt from bfe.projektierungszonen_starkstromanlagen_v2_0_oereb WHERE rechtsstatus IN ('AenderungOhneVorwirkung', 'AenderungMitVorwirkung')) as data using unique stabil_id using srid=2056"
    DATA "the_geom from (SELECT  bgdi_id, the_geom, stabil_id, text_aussage_%lang% as text_aussage, text_thema_%lang% as text_thema, text_rechtsstatus_%lang% as text_rechtsstatus, publiziertab, publiziertbis, text_amt_%lang% as text_amt from bfe.projektierungszonen_starkstromanlagen_v2_0_oereb WHERE rechtsstatus LIKE 'inKraft') as data using unique stabil_id using srid=2056"
    DATA "the_geom from (SELECT * from bfe.rohrleitungen_konsultation where klasse like 'Konsbereich') as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * from bfe.rohrleitungen_konsultation where klasse like 'Lage') as data using unique bgdi_id using srid=2056"
    DATA "the_geom from  (SELECT * from bfe.sachplan_geologie_tiefenlager_pm_area) as data using unique pmarea_id using srid=2056"
    DATA "the_geom from  (SELECT * from bfe.sachplan_geologie_tiefenlager_pm_area) as data using unique pmarea_id using srid=2056"
    DATA "the_geom from  (SELECT *  from bfe.sachplan_geologie_tiefenlager_pm_pt) as data using unique pmpt_id using srid=2056"
    DATA "the_geom from  (SELECT *  from bfe.sachplan_geologie_tiefenlager_fac_pt) as data using unique facpt_id using srid=2056"
    DATA "the_geom from  (SELECT *  from bfe.geologische_tiefenlager_fac) as data using unique stabil_id using srid=2056"
    DATA "the_geom from  (SELECT *  from bfe.geologische_tiefenlager ORDER BY ordering desc) as data using unique stabil_id using srid=2056"
    DATA "the_geom from  (SELECT stabil_id, geomtype, coordlevel_tid, ordering, meastype_tid, ST_Buffer(the_geom,-100) as the_geom from bfe.geologische_tiefenlager ORDER BY ordering desc) as data using unique stabil_id using srid=2056"
    DATA "the_geom from  (SELECT *  from bfe.geologische_tiefenlager_raster ORDER BY ordering desc) as data using unique stabil_id using srid=2056"
    DATA "the_geom from  (SELECT stabil_id, geomtype, coordlevel_tid, ordering, meastype_tid, ST_Buffer(the_geom,-25) as the_geom from bfe.geologische_tiefenlager_raster ORDER BY ordering desc) as data using unique stabil_id using srid=2056"
    DATA "the_geom from  (SELECT *  from bfe.geologische_tiefenlager_raster ORDER BY ordering desc) as data using unique stabil_id using srid=2056"
    DATA "the_geom from  (SELECT stabil_id, geomtype, coordlevel_tid, ordering, meastype_tid, ST_Buffer(the_geom,-25) as the_geom from bfe.geologische_tiefenlager_raster ORDER BY ordering desc) as data using unique stabil_id using srid=2056"
    DATA "the_geom from  (SELECT * from bfe.suel_fac_anhorung WHERE geom_type like '%point%' ORDER BY stabil_id) as data using unique stabil_id using srid=2056"
    DATA "the_geom from  (SELECT *  from bfe.suel_fac_r_anhorung WHERE geom_type like '%point%' ORDER BY stabil_id) as data using unique stabil_id using srid=2056"
    DATA "the_geom_line FROM (SELECT * FROM bfe.suel_pl_v_anhorung WHERE geom_type like '%point%') as data using unique stabil_id using srid=2056"
    DATA "the_geom_line FROM (SELECT * FROM bfe.suel_pl_r_anhorung WHERE geom_type like '%point%') as data using unique stabil_id using srid=2056"
    DATA "the_geom from  (SELECT * from bfe.suel_pl_anhorung WHERE geom_type like '%area%') as data using unique stabil_id using srid=2056"
    DATA "the_geom from  (SELECT * from bfe.suel_pl_r_anhorung WHERE geom_type like '%area%') as data using unique stabil_id using srid=2056"
    DATA "the_geom from  (SELECT * from bfe.suel_pl_anhorung WHERE geom_type like '%line%') as data using unique stabil_id using srid=2056"
    DATA "the_geom from  (SELECT * from bfe.suel_fac_kraft WHERE geom_type like '%point%' ORDER BY stabil_id) as data using unique stabil_id using srid=2056"
    DATA "the_geom from  (SELECT *  from bfe.suel_fac_r_kraft WHERE geom_type like '%point%' ORDER BY stabil_id) as data using unique stabil_id using srid=2056"
    DATA "the_geom_line FROM (SELECT * FROM bfe.suel_pl_v_kraft WHERE geom_type like '%point%') as data using unique stabil_id using srid=2056"
    DATA "the_geom_line FROM (SELECT * FROM bfe.suel_pl_r_kraft WHERE geom_type like '%point%') as data using unique stabil_id using srid=2056"
    DATA "the_geom from  (SELECT * from bfe.suel_pl_kraft WHERE geom_type like '%area%') as data using unique stabil_id using srid=2056"
    DATA "the_geom from  (SELECT * from bfe.suel_pl_r_kraft WHERE geom_type like '%area%') as data using unique stabil_id using srid=2056"
    DATA "the_geom from  (SELECT * from bfe.suel_pl_kraft WHERE geom_type like '%line%') as data using unique stabil_id using srid=2056"
    DATA "the_geom from (SELECT * from bfe.solarenergie_availability) as blabla using unique av_id using srid=2056"
    DATA "the_geom from (SELECT * from bfe.view_solarenergie_daecher_gs) as blabla using unique df_uid using srid=2056"
    DATA "the_geom from (SELECT * from bfe.solarenergie_fassaden_availability) as blabla using unique av_id using srid=2056"
    DATA "the_geom from (SELECT * from bfe.view_solarenergie_fassaden_gs) as blabla using unique ff_uuid using srid=2056"
    DATA "the_geom FROM (SELECT the_geom, wastanumber, name, location, canton, beginningofoperation, endofoperation, hydropowerplanttype, hydropowerplanttype_de, hydropowerplanttype_fr, hydropowerplanttype_it, hydropowerplantoperationalstatus_de, hydropowerplantoperationalstatus_fr, hydropowerplantoperationalstatus_it FROM bfe.statistik_wasserkraftanlagen_powerplant order by display_order) AS blabla using unique wastanumber using srid=2056"
    DATA "the_geom FROM (SELECT damname,damtype, damtype_de, damtype_fr, damtype_en, dam_stabil_id, damheight, crestlevel, crestlength, facilityname, beginningofoperation, startsupervision, reservoirname, impoundmentvolume, impoundmentlevel, storagelevel, facaim_fr, facaim_en, facaim_de, has_picture, baujahr, the_geom FROM bfe.stauanlagen_bundesaufsicht) AS blabla using unique dam_stabil_id using srid=2056"
    DATA "the_geom FROM (SELECT * FROM bfe.stauanlagen_bundesaufsicht limit 0) AS blabla USING UNIQUE bgdi_id USING srid=2056"
    DATA "the_geom from (SELECT * from bfe.thermische_netze) as geometrie using unique xtf_id using srid=2056"
    DATA "the_geom FROM (SELECT * FROM bfe.waermepotential_gewaesser WHERE geometrytype(the_geom) = 'MULTIPOLYGON') AS blabla USING UNIQUE bgdi_id USING srid=2056"
    DATA "the_geom FROM (SELECT * FROM bfe.waermepotential_gewaesser WHERE geometrytype(the_geom) = 'MULTILINESTRING') AS blabla USING UNIQUE bgdi_id USING srid=2056"
    DATA "the_geom FROM (SELECT * FROM bfe.waermepotential_gewaesser WHERE geometrytype(the_geom) = 'POINT') AS blabla USING UNIQUE bgdi_id USING srid=2056"
    DATA "the_geom FROM (SELECT the_geom, bgdi_id, xtf_id, kategorie as fac_type, fac_name, kategorie, tur_alti, power, tur_year, status FROM bfe.windenergieanlagen %filter% ORDER BY kategorie ASC) AS blabla using unique xtf_id using srid=2056"
    DATA "the_geom FROM (SELECT *, (survey::text || '-01-01'::text)::date AS bgdi_date FROM bfs.arealstatistik_nolc) AS data USING UNIQUE bgdi_id USING srid=2056"
    DATA "the_geom FROM (SELECT *, (survey::text || '-01-01'::text)::date AS bgdi_date FROM bfs.arealstatistik_nolc where change = 0) AS data USING UNIQUE bgdi_id USING srid=2056"
    DATA "the_geom FROM (SELECT *, (survey::text || '-01-01'::text)::date AS bgdi_date FROM bfs.arealstatistik_nolc where change = 1) AS data USING UNIQUE bgdi_id USING srid=2056"
    DATA "the_geom from (select bgdi_id, the_geom from bfs.arealstatistik_std_1997) as blabla USING UNIQUE bgdi_id USING srid=2056"
    DATA "the_geom from (select bgdi_id, fj85, fj97, fj09, id_arealstatistik_nolu_97 as id_lu97_16, id_arealstatistik_nolu_85, id_arealstatistik_nolu_97, id_arealstatistik_nolu_09, the_geom from bfs.arealstatistik_nolu_1997) as blabla USING UNIQUE bgdi_id USING srid=2056"
    DATA "the_geom from (select bgdi_id, the_geom from bfs.arealstatistik_std_1997) as blabla USING UNIQUE bgdi_id USING srid=2056"
    DATA "the_geom from (select bgdi_id, fj85, fj97, fj09, id_arealstatistik_nolu_09 as id_lu09_16, id_arealstatistik_nolu_85, id_arealstatistik_nolu_97, id_arealstatistik_nolu_09, the_geom from bfs.arealstatistik_nolu_2009) as blabla USING UNIQUE bgdi_id USING srid=2056"
    DATA "the_geom FROM (SELECT *, (survey::text || '-01-01'::text)::date AS bgdi_date FROM bfs.arealstatistik_std) AS data USING UNIQUE bgdi_id USING srid=2056"
    DATA "the_geom FROM (SELECT *, (survey::text || '-01-01'::text)::date AS bgdi_date FROM bfs.arealstatistik_std where change = 0) AS data USING UNIQUE bgdi_id USING srid=2056"
    DATA "the_geom FROM (SELECT *, (survey::text || '-01-01'::text)::date AS bgdi_date FROM bfs.arealstatistik_std where change = 1) AS data USING UNIQUE bgdi_id USING srid=2056"
    DATA "the_geom from (%query%) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (%query%) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (%query%) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (%query%) as data using unique bgdi_id using srid=2056"
    DATA "the_geom FROM (
    DATA "the_geom from  (SELECT * from bfs.gebaeude_wohnungs_register_waermequelle) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (%query%) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (%query%) as data using unique bgdi_id using srid=2056"
    DATA "the_geom FROM (SELECT *, linkdownload_de as linkdownload, typ1 as typ, linkbild as quickview FROM bfs.landschaftswandel) AS data USING UNIQUE bgdi_id USING srid=2056"
    DATA "the_geom from (%query%) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (%query%) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (%query%) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT the_geom, berg_alp, gid from blw.berg_und_alpprodutkte) as data using unique gid using srid=2056"
    DATA "the_geom from (SELECT the_geom, berg_alp, gid from blw.berg_und_alpprodutkte) as data using unique gid using srid=2056"
    DATA "the_geom from (SELECT the_geom, grundigkei, ku_code, bgdi_id, eignungsei, farbe from blw.bodeneignung) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT the_geom, ku_code, bgdi_id, eignungsei, farbe from blw.bodeneignung) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT the_geom, eignungsei, farbe, bgdi_id from blw.bodeneignung) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT the_geom, nahrstoff, bgdi_id, eignungsei, farbe from blw.bodeneignung) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT the_geom, skelett, bgdi_id, eignungsei, farbe from blw.bodeneignung) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT the_geom, vernass, bgdi_id, eignungsei, farbe from blw.bodeneignung) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT the_geom, wasserdurc, bgdi_id, eignungsei, farbe from blw.bodeneignung) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT the_geom, eignungsei, wasserspei, ku_code, bgdi_id, farbe from blw.bodeneignung) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT %geometry% as the_geom, bgdi_id from blw.feldblockkarte ) as data using unique bgdi_id using srid=2056"
    DATA "the_geom FROM (SELECT bgdi_id, hl_neigung, the_geom FROM blw.hanglagen_abschwem) AS data USING UNIQUE bgdi_id USING srid=2056"
    DATA "the_geom from (SELECT the_geom, fu_code, bgdi_id, klimeig_be, zone from blw.klimaeignung) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT the_geom, ge_code, bgdi_id, klimeig_be, zone from blw.klimaeignung) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT the_geom, ka_code, bgdi_id, klimeig_be, zone from blw.klimaeignung) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT the_geom, koe_code, bgdi_id, klimeig_be, zone from blw.klimaeignung) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT the_geom, ku_code, bgdi_id, klimeig_be, zone from blw.klimaeignung) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT the_geom, sp_code, bgdi_id, klimeig_be, zone from blw.klimaeignung) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT the_geom, klimeig_id, bgdi_id, klimeig_be, zone from blw.klimaeignung) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT the_geom, zw_code, bgdi_id, klimeig_be, zone from blw.klimaeignung) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from blw.landw_zonenkarte using unique bgdi_id using srid=2056"
    DATA "the_geom FROM (SELECT milchreg, reg, the_geom FROM blw.milchmarktregionen) AS data USING UNIQUE reg USING srid=2056"
    DATA "the_geom from (SELECT the_geom, ni_code, bgdi_id, klimeig_be, zone from blw.klimaeignung) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT the_geom, bgdi_id, objektcode, eintrag, objekt_d as objekt FROM blw.ursprungsbezeichnungen_fleisch) AS blabla USING UNIQUE bgdi_id USING srid=2056"
    DATA "the_geom from (SELECT the_geom, bgdi_id, objektcode, eintrag, objekt_d as objekt FROM blw.ursprungsbezeichnungen_kaese) AS blabla USING UNIQUE bgdi_id USING srid=2056"
    DATA "the_geom from (SELECT the_geom, bgdi_id, objektcode, eintrag, objekt_d as objekt FROM blw.ursprungsbezeichnungen_konditoreiwaren) AS blabla USING UNIQUE bgdi_id USING srid=2056"
    DATA "the_geom from (SELECT the_geom, bgdi_id, objektcode, eintrag, objekt_d as objekt FROM blw.ursprungsbezeichnungen_pflanzen) AS blabla USING UNIQUE bgdi_id USING srid=2056"
    DATA "the_geom from (SELECT the_geom, bgdi_id, objektcode, eintrag, objekt_d as objekt FROM blw.ursprungsbezeichnungen_spirituosen) AS blabla USING UNIQUE bgdi_id USING srid=2056"
    DATA "the_geom from (SELECT * from ensi.zonenplan_kernanlagen order by zwischenlager asc, zone desc) as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT *, eigentuemer_de as eigentuemer, parameter_de as parameter, standorttyp_de as standorttyp, stationhoehe as stationshoehe, stationtyp_de as stationtyp from meteoschweiz.klimaszenarien_raumklima order by bgdi_id) AS bla USING UNIQUE bgdi_id USING SRID=2056"
    DATA "the_geom from (SELECT *, et_fromatt_1100 as aeromagnetisme from nagra.aeromagnetische_karte WHERE type_aeromagnetismus like 'am_1100') as data using unique et_id using srid=2056"
    DATA "the_geom from (SELECT *, et_fromatt_1500 as aeromagnetisme  from nagra.aeromagnetische_karte WHERE type_aeromagnetismus like 'am_1500') as data using unique et_id using srid=2056"
    DATA "the_geom FROM (SELECT bgdi_id,nummer,name,the_geom FROM pronatura.naturschutzgebiete) as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom from  (SELECT *, obj_gisflaeche as gisflaeche, obj_gisteilobjekt as gisteilobjekt from pronatura.waldreservate) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (select * from sbfi.sachplan_cern_anhoerung_plm_poly) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (select * from sbfi.sachplan_cern_anhoerung_fac_line) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (select * from sbfi.sachplan_cern_anhoerung_fac_pnt) as data using unique bgdi_id using srid=2056"
    DATA "the_geom FROM (SELECT * FROM sem.sachplan_asyl_plmeasures_r_anhorung WHERE geom_type like '%area%') as data using unique stabil_id using srid=2056"
    DATA "the_geom FROM (SELECT * FROM sem.sachplan_asyl_plmeasures_anhorung WHERE geom_type like '%area%') as data using unique stabil_id using srid=2056"
    DATA "the_geom from  (SELECT stabil_id, plname_de, meastype_text_de, meastype_tid, coordlevel_bid, coordlevel_text_de, plstatus_text_de, validfrom, validuntil description_text_de, document_title, document_web, objectname_de, the_geom from sem.sachplan_asyl_plmeasures_anhorung WHERE geom_type like '%point%') as data using unique stabil_id using srid=2056"
    DATA "the_geom FROM (SELECT * FROM sem.sachplan_asyl_facilities_anhorung WHERE geom_type like '%point%') as data using unique stabil_id using srid=2056"
    DATA "the_geom FROM (SELECT * FROM sem.sachplan_asyl_plmeasures_r_kraft WHERE geom_type like '%area%') as data using unique stabil_id using srid=2056"
    DATA "the_geom FROM (SELECT * FROM sem.sachplan_asyl_plmeasures_kraft WHERE geom_type like '%area%') as data using unique stabil_id using srid=2056"
    DATA "the_geom FROM (SELECT * FROM sem.sachplan_asyl_plmeasures_kraft WHERE geom_type like '%point%') as data using unique stabil_id using srid=2056"
    DATA "the_geom FROM (SELECT * FROM sem.sachplan_asyl_facilities_kraft WHERE geom_type like '%point%') as data using unique stabil_id using srid=2056"
    DATA "the_geom FROM (Select %query% from vd.addressverzeichnis) nofx USING UNIQUE bgdi_id USING SRID=2056"
    DATA "the_geom FROM ( select str_esid as id, str_official, str_modified, bgdi_id, str_esid, stn_label, zip_label, com_name, com_fosnr, str_valid, str_type, str_status, %geometry% as the_geom  from vd.streetnames WHERE bgdi_geomtype = 'line') as blabla USING UNIQUE bgdi_id USING SRID=2056"
    DATA "the_geom FROM ( select str_esid as id, str_official, str_modified, bgdi_id, str_esid, replace(stn_label, '/', '/|') as stn_label, zip_label, com_name, com_fosnr, str_valid, str_status, str_type, %geometry% as the_geom  from vd.streetnames WHERE bgdi_geomtype = 'poly') as blabla USING UNIQUE bgdi_id USING SRID=2056"
    DATA "the_geom FROM ( select str_esid as id, str_official, str_modified, str_status, bgdi_id, str_esid, stn_label, zip_label, com_name, com_fosnr, str_valid, str_type, the_geom  from vd.streetnames WHERE bgdi_geomtype = 'line' limit 0) as blabla USING UNIQUE bgdi_id USING SRID=2056"
    DATA "the_geom from (SELECT * FROM datenstand.view_gridstand_bk200) as blabla using unique pk_product using srid=2056"
    DATA "the_geom from (SELECT * FROM datenstand.view_gridstand_bk200) as blabla using unique pk_product using srid=2056"
    DATA "the_geom from (SELECT * FROM datenstand.view_gridstand_bk200) as blabla using unique pk_product using srid=2056"
    DATA "the_geom from (SELECT pk_product, s_title_%lang% as label, the_geom FROM datenstand.view_gridstand_bk200) as blabla using unique pk_product using srid=2056"
    DATA "the_geom  from (Select * from  geodaesie.dreiecksvermaschung order by type asc) as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT the_geom, no, url, bgdi_id from fpds.agnes) as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom from (
    DATA "the_geom from (
    DATA "the_geom from (
    DATA "the_geom from (
    DATA "the_geom from (SELECT * FROM geol.view_gridstand_gas25) as blabla using unique pk_product using srid=2056"
    DATA "the_geom from (SELECT * FROM geol.view_gridstand_gas25) as blabla using unique pk_product using srid=2056"
    DATA "the_geom from (SELECT * FROM geol.view_gridstand_gas25) as blabla using unique pk_product using srid=2056"
    DATA "the_geom from (SELECT * FROM geol.view_gridstand_gas25) as blabla using unique pk_product using srid=2056"
    DATA "the_geom from (SELECT id, zoomstufe,the_geom  FROM geodaesie.geoidmodell_ch1903_line %filter%) as blabla using unique id using srid=2056"
    DATA "the_geom from (SELECT id, zoomstufe,the_geom  FROM geodaesie.geoidmodell_ch1903_plg %filter%) as blabla using unique id using srid=2056"
    DATA "the_geom from (SELECT id, zoomstufe,the_geom  FROM geodaesie.geoidmodell_etrs89_line %filter%) as blabla using unique id using srid=2056"
    DATA "the_geom from (SELECT id, zoomstufe,the_geom  FROM geodaesie.geoidmodell_etrs89_plg %filter%) as blabla using unique id using srid=2056"
    DATA "the_geom from (SELECT * from geol.bohrungen_tiefer_500) as data using unique bgdi_id using srid=2056"
    DATA "the_geom FROM geol.dosisleistung_terrestrisch USING UNIQUE bgdi_id USING SRID=2056"
    DATA "the_geom from (SELECT bgdi_id, ads_name, the_geom   FROM geol.eiszeit_lgm500) as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * from geol.felslabore) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT bgdi_id, nr, titel, jahr, autor, the_geom FROM geol.generalkarte_ggk200_meta) as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * from geol.geocover_line_aux) as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * from geol.geocover_point_hydro) as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * from geol.geocover_point_geol) as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * from geol.geocover_point_drill) as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * from geol.geocover_point_info) as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * from geol.geocover_point_struct) as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * from geol.geocover_polygon_aux_1) as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * from geol.geocover_polygon_aux_2) as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * from geol.geocover_polygon_main) as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom FROM (SELECT bgdi_id as id, bgdi_id, msh_map_nb, msh_map_ti, msh_scale, msh_edit, msh_versio, msh_basis, msh_author, msh_rev, concat(msh_map_nb,'&', msh_titel) as label, the_geom FROM geol.geocover_meta ) as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT gid, et_fromatt, the_geom  FROM geol.geodaesie_bouguer_anomalien) as blabla using unique gid using srid=2056"
    DATA "the_geom from (SELECT gid, id, et_fromatt, the_geom  FROM geol.schwerekarte_isostatische_anomalien) as blabla using unique gid using srid=2056"
    DATA "the_geom FROM (SELECT post_id as id, * FROM geol.v_erlebnis_anfrage) AS foo USING UNIQUE bgdi_id USING srid=2056"
    DATA "the_geom FROM (SELECT  post_id as id, * FROM geol.v_erlebnis_naechste) AS foo USING UNIQUE bgdi_id USING srid=2056"
    DATA "the_geom from (SELECT * from geol.geologische_3dmodelle) as geometrie using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT bgdi_id, category, shape_area, st_centroid(the_geom) as the_geom from geol.geologische_3dmodelle) as geometrie using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT fid as id, id as gid, fid, type_de, type_fr, the_geom from geol.geologische_karte_line) as blabla using unique fid using srid=2056"
    DATA "the_geom from (SELECT * from geol.geologische_karte_plg) as blabla using unique id using srid=2056"
    DATA "the_geom from (SELECT * from geol.geologischer_atlas_line_aux) as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * from geol.geologischer_atlas_point_hydro) as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * from geol.geologischer_atlas_point_geol) as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * from geol.geologischer_atlas_point_drill) as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * from geol.geologischer_atlas_point_info) as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * from geol.geologischer_atlas_point_struct) as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * from geol.geologischer_atlas_polygon_aux_1) as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * from geol.geologischer_atlas_polygon_aux_2) as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * from geol.geologischer_atlas_polygon_main) as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT bgdi_id, nummer, nummer as s_map_number, name, name as s_title_de, massstab, massstab as scale, ausgabejahr as release, autoren, the_geom  FROM geol.geologischer_atlas_raster_meta) as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT bgdi_id, ga25_id, ga25_name, ga25_no, ga25_edition, plate_no, section_no, section_type_en as section_type, scale, vert_exag, author, link_original, link_onlineshop_en as link_onlineshop,swissgeol_link_en as swissgeol_link, the_geom from geol.geologischer_atlas_profil) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT bgdi_id, nummer, name, massstab, ausgabejahr, autoren, the_geom FROM geol.geologischer_atlas_vector_meta) as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT bgdi_id, nummer, the_geom FROM geol.geologischer_atlas_vector_meta) as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT bgdi_id, name, the_geom FROM geol.geologischer_atlas_vector_meta) as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT *  from geol.geomol_isotherme_100_elev) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT *  from geol.geomol_isotherme_100_fault) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * from geol.geomol_isotherme_150_elev) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT *  from geol.geomol_isotherme_150_fault) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT *  from geol.geomol_isotherme_60_elev) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT *  from geol.geomol_isotherme_60_fault) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT *  from geol.geomol_temperaturmodell_eingangsdaten_pt) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT *  from geol.geomol_temperaturmodell_eingangsdaten_geowatt) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT *  from geol.geomol_temperaturmodell_eingangsdaten_lim) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT *  from geol.geomol_temperatur_muschelkalk_elev) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT *  from geol.geomol_temperatur_muschelkalk_fault) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT *  from geol.geomol_temperatur_omalm_elev) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT *  from geol.geomol_temperatur_omalm_fault) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * from geol.geomol_temperatur_omm_elev) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT *  from geol.geomol_temperatur_omm_fault) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT *  from geol.geomol_temperaturverteilung_1000) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT *  from geol.geomol_temperaturverteilung_1500) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT *  from geol.geomol_temperaturverteilung_2000) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT *  from geol.geomol_temperaturverteilung_3000) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT *  from geol.geomol_temperaturverteilung_4000) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT *  from geol.geomol_temperaturverteilung_500) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT bgdi_id, ads_id, ads_name, the_geom FROM geol.geomorphologie) as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT gid, id, et_fromatt, the_geom  FROM geol.gravimetrie_aeromagnetik_jura) as blabla using unique gid using srid=2056"
    DATA "the_geom from (SELECT gid, id, et_fromatt, et_fromatt as et_fromatt_ch, the_geom  FROM geol.gravimetrie_aeromagnetik_ch) as blabla using unique gid using srid=2056"
    DATA "the_geom from (SELECT gid, declina_c_, magne, the_geom FROM geol.geophysik_deklination) as blabla using unique gid using srid=2056"
    DATA "the_geom from (SELECT gid, id, contour, contour as contour_geothermie, name, the_geom  FROM geol.geophysik_geothermie) as blabla using unique gid using srid=2056"
    DATA "the_geom from (SELECT gid, contour, the_geom FROM geol.geophysik_inklination) as blabla using unique gid using srid=2056"
    DATA "the_geom from (SELECT gid, id, contour, contour as contour_totalintensitaet, the_geom FROM geol.geophysik_totalintensitaet) as blabla using unique gid using srid=2056"
    DATA "the_geom FROM (SELECT * FROM geol.v_erlebnis_sites) AS foo USING UNIQUE bgdi_id USING srid=2056"
    DATA "the_geom FROM (select * from geol.gk500_genese) as foo using unique bgdi_id using srid=2056"
    DATA "the_geom FROM (select * from geol.gk500_gesteinsklassierung) as foo using unique bgdi_id using srid=2056 "
    DATA "the_geom from (select *, bgdi_tooltip_de as gk500_litho FROM geol.gk500_lithologie_hauptgruppen) as foo using unique bgdi_id using srid=2056"
    DATA "the_geom from (select *, area_name as name from geol.geotechnik_mineralische_rohstoffe200) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT id,leg_nr,gesteinsgr,gestein,the_geom FROM geol.geotechnik_steinbrueche_1915) as blabla using unique id using srid=2056"
    DATA "the_geom from (SELECT id,leg_nr,gesteinsgr,gestein,the_geom FROM geol.geotechnik_steinbrueche_1965) as blabla using unique id using srid=2056"
    DATA "the_geom from (SELECT id,leg_nr,gesteinsgr,gestein,the_geom FROM geol.geotechnik_steinbrueche_1980) as blabla using unique id using srid=2056"
    DATA "the_geom from (SELECT id,leg_nr,gesteinsgr,gestein,the_geom FROM geol.geotechnik_steinbrueche_1995) as blabla using unique id using srid=2056"
    DATA "the_geom FROM (SELECT * FROM geol.geotechnik_steine_historische_bauwerke ORDER BY size desc) as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT id,stoff,jahr,the_geom FROM geol.geotechnik_zementindustrie where jahr='1965') as blabla using unique id using srid=2056"
    DATA "the_geom from (SELECT id,stoff,jahr,the_geom FROM geol.geotechnik_zementindustrie where jahr='1995') as blabla using unique id using srid=2056"
    DATA "the_geom from (SELECT id,nummer_a,gmde,name_ansi,ziegelei_2,the_geom FROM geol.geotechnik_ziegeleien_1907) as blabla using unique id using srid=2056"
    DATA "the_geom from (SELECT *, titel_de as titel, weblink_de as weblink from geol.geothermische_potenzialstudien) as data using unique bgdi_id using srid=2056"
    DATA "the_geom FROM (SELECT objectid, nom, nom as name, nummer, fix_id, the_geom FROM geol.geotope_plg) AS blabla using unique objectid using srid=2056"
    DATA "the_geom FROM geol.view_stand_kantonale_geotope USING UNIQUE bgdi_id USING srid=2056"
    DATA "the_geom FROM (SELECT objectid, fix_id, nom, nom as name, nummer, the_geom FROM geol.geotope_pkt) AS blabla using unique objectid using srid=2056"
    DATA "the_geom FROM (SELECT * FROM geol.erlebnis_geowege) AS data using unique bgdi_id using srid=2056"
    DATA "the_geom FROM (
    DATA "the_geom from (SELECT * from geol.gesteinsdichte) AS bla USING UNIQUE bgdi_id USING SRID=2056"
    DATA "the_geom from (
    DATA "the_geom from (
    DATA "the_geom from (
    DATA "the_geom from (
    DATA "the_geom from (
    DATA "the_geom from (
    DATA "the_geom from (
    DATA "the_geom from (SELECT  gid, bgdi_data_status, count, pdf_url, year, doccreation_date, ST_LineSubstring(the_geom, %splitdistance%*n/length,
    DATA "the_geom from (
    DATA "the_geom from (SELECT * from geol.gletscherausdehnung WHERE layer_id not in ('6_SGI_2016_icedivides') order by layer_id) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * FROM geol.gletscherausdehnung WHERE layer_id in ('6_SGI_2016_icedivides')) as data using unique bgdi_id using srid=2056"
    DATA "the_geom FROM geol.gletschermaechtigkeit_ice_thinkness using unique bgdi_id using srid=2056"
    DATA "the_geom FROM (SELECT *, prf_name AS gpr_prf_name, max_thik AS gpr_max_thik FROM geol.gletschermaechtigkeit_gpr_profiles) AS data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * FROM geol.gravimetrisch_messpunkte) AS data using unique bgdi_id using srid=2056"
    DATA "the_geom from (select * from geol.geologie_reflexionsseismik_line) as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom from (select * from  geol.geologie_reflexionsseismik_poly) as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * FROM geol.rohstoffe_gebrochenegesteineabbau ORDER BY stid) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * FROM geol.rohstoffe_gipsabbauverarbeitung) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * FROM geol.rohstoffe_industriemin) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * FROM geol.rohstoffe_kohlenbitumenerdgas) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * FROM geol.rohstoffe_naturwerksteineabbau ORDER BY stid) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * FROM geol.rohstoffe_salzabbauverarbeitung) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * FROM geol.rohstoffe_vererz) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * FROM geol.rohstoffe_zementabbauverarbeitung_poly where type like 'poly_occ') as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * FROM geol.rohstoffe_zementabbauverarbeitung_poly where type like 'poly_mining') as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * FROM geol.rohstoffe_zementabbauverarbeitung where type like 'mining') as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * FROM geol.rohstoffe_zementabbauverarbeitung where type like 'production') as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * FROM geol.rohstoffe_zementabbauverarbeitung where type like 'production') as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * FROM geol.rohstoffe_ziegel_abbau_poly where type like 'poly_occ') as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * FROM geol.rohstoffe_ziegel_abbau_poly where type like 'poly_mining') as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * FROM geol.rohstoffe_ziegel_abbau where type like 'mining') as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * FROM geol.rohstoffe_ziegel_abbau where type like 'production') as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * FROM geol.rohstoffe_ziegel_abbau where type like 'production') as data using unique bgdi_id using srid=2056"
    DATA "the_geom FROM (SELECT * FROM geol.rohstoffe_ziegel_verarbeitung ORDER BY stid) AS blabla USING UNIQUE bgdi_id USING srid=2056"
    DATA "the_geom FROM (SELECT bgdi_id, gsk_nr, titel, massstab, jahrgang, autoren, the_geom FROM geol.spezialkarten_raster_meta) as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom FROM (SELECT bgdi_id, st_boundary(the_geom) AS the_geom FROM geol.spezialkarten_raster_meta) as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom FROM (SELECT bgdi_id, gsk_nr, titel, jahrgang, massstab, autoren, the_geom FROM geol.spezialkarten_vector_meta) as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom FROM geol.dosisleistung_terrestrisch USING UNIQUE bgdi_id USING SRID=2056"
    DATA "the_geom from (SELECT * FROM geol.tiefengeothermie_projekte_plg) AS bla USING UNIQUE bgdi_id USING SRID=2056"
    DATA "the_geom from (SELECT * from geol.tiefengeothermie_projekte_pkt) AS bla USING UNIQUE bgdi_id USING SRID=2056"
    DATA "the_geom from bezirke25 using unique gid using srid=2056"
    DATA "the_geom from bezirke25"
    DATA "the_geom from bezirke25"
    DATA "the_geom from bezirke25"
    DATA "the_geom from gg25 using unique id using srid=2056"
    DATA "the_geom from gg25"
    DATA "the_geom from gg25"
    DATA "the_geom from gg25"
    DATA "the_geom from kantone25 using unique kantonsnr using srid=2056"
    DATA "the_geom from kantone25"
    DATA "the_geom from kantone25"
    DATA "the_geom from kantone25"
    DATA "the_geom from (SELECT * FROM datenstand.view_gridstand_gk300) as blabla using unique pk_product using srid=2056"
    DATA "the_geom from (SELECT * FROM datenstand.view_gridstand_gk300) as blabla using unique pk_product using srid=2056"
    DATA "the_geom from (SELECT pk_product, s_title_%lang% as label, the_geom FROM datenstand.view_gridstand_gk300) as blabla using unique pk_product using srid=2056"
    DATA "the_geom from (SELECT * FROM geol.view_gridstand_gkba500) as blabla using unique pk_product using srid=2056"
    DATA "the_geom from (SELECT * FROM geol.view_gridstand_gkba500) as blabla using unique pk_product using srid=2056"
    DATA "the_geom from (SELECT pk_product, s_title_%lang% as label, the_geom FROM geol.view_gridstand_gkba500) as blabla using unique pk_product using srid=2056"
    DATA "the_geom from (SELECT * FROM geol.view_gridstand_gkg500) as blabla using unique pk_product using srid=2056"
    DATA "the_geom from (SELECT * FROM geol.view_gridstand_gkg500) as blabla using unique pk_product using srid=2056"
    DATA "the_geom from (SELECT pk_product, s_title_%lang% as label, the_geom FROM geol.view_gridstand_gkg500) as blabla using unique pk_product using srid=2056"
    DATA "the_geom from (SELECT * FROM geol.view_gridstand_gklgm500) as blabla using unique pk_product using srid=2056"
    DATA "the_geom from (SELECT * FROM geol.view_gridstand_gklgm500) as blabla using unique pk_product using srid=2056"
    DATA "the_geom from (SELECT pk_product, s_title_%lang% as label, the_geom FROM geol.view_gridstand_gklgm500) as blabla using unique pk_product using srid=2056"
    DATA "the_geom from (SELECT * FROM geol.view_gridstand_gkt500) as blabla using unique pk_product using srid=2056"
    DATA "the_geom from (SELECT * FROM geol.view_gridstand_gkt500) as blabla using unique pk_product using srid=2056"
    DATA "the_geom from (SELECT pk_product, s_title_%lang% as label, the_geom FROM geol.view_gridstand_gkt500) as blabla using unique pk_product using srid=2056"
    DATA "the_geom from (SELECT * FROM geol.view_gridstand_gkwvor500) as blabla using unique pk_product using srid=2056"
    DATA "the_geom from (SELECT * FROM geol.view_gridstand_gkwvor500) as blabla using unique pk_product using srid=2056"
    DATA "the_geom from (SELECT pk_product, s_title_%lang% as label, the_geom FROM geol.view_gridstand_gkwvor500) as blabla using unique pk_product using srid=2056"
    DATA "the_geom from (SELECT * FROM geol.view_gridstand_gsk) as blabla using unique pk_product using srid=2056"
    DATA "the_geom from (SELECT * FROM geol.view_gridstand_gsk) as blabla using unique pk_product using srid=2056"
    DATA "the_geom from (SELECT * FROM geol.view_gridstand_gsk) as blabla using unique pk_product using srid=2056"
    DATA "the_geom from (SELECT * FROM geol.view_gridstand_gsk) as blabla using unique pk_product using srid=2056"
    DATA "the_geom from (SELECT *, round(contour::numeric,1) as contour_round FROM geodaesie.hebungsraten_line) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * FROM geodaesie.hebungsraten_pkt order by klasse) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (
    DATA "the_geom from (SELECT *, kbbez AS name FROM datenstand.view_gridstand_dufour) as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT *, kbbez AS name FROM datenstand.view_gridstand_dufour) as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT *, kbbez AS name FROM datenstand.view_gridstand_dufour) as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT *, kbbez AS name FROM datenstand.view_gridstand_dufour) as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom from (
    DATA "the_geom from (SELECT gid, tilenumber, datenstand, produkt, the_geom FROM datenstand.view_gridstand_datenhaltung_siegfried_erst) as blabla using unique gid using srid=2056"
    DATA "the_geom from (SELECT *, kbbez AS name FROM datenstand.view_gridstand_siegfried_ta25) as blabla using unique bgdi_id using  srid=2056"
    DATA "the_geom from (SELECT *, kbbez AS name FROM datenstand.view_gridstand_siegfried_ta25) as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT *, kbbez AS name FROM datenstand.view_gridstand_siegfried_ta25) as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT *, kbbez AS name FROM datenstand.view_gridstand_siegfried_ta25) as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT *, kbbez AS name FROM datenstand.view_gridstand_siegfried_ta50) as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT *, kbbez AS name FROM datenstand.view_gridstand_siegfried_ta50) as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT *, kbbez AS name FROM datenstand.view_gridstand_siegfried_ta50) as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT *, kbbez AS name FROM datenstand.view_gridstand_siegfried_ta50) as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT tile_id, flightyear AS datenstand, flightyear AS release, resolution, the_geom FROM datenstand.view_gridstand_datenhaltung_swissimage_dop10_tilecache) as blabla using unique tile_id using srid=2056"
    DATA "the_geom from (SELECT tile_id, flightyear AS datenstand, resolution, the_geom FROM datenstand.view_gridstand_datenhaltung_swissimage_dop10_tilecache) as blabla using unique tile_id using srid=2056"
    DATA "the_geom from (SELECT bgdi_id, tilenumber, datenstand::integer as flightdate,produkt, the_geom FROM datenstand.view_gridstand_datenhaltung_swissimage_tilecache) as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT the_geom, bgdi_id, hikingtype, bridgetype, tunneltype from karto.wanderwege_swissmap ORDER BY bgdi_id ASC) as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom from  (
    DATA "the_geom from (SELECT * FROM datenstand.view_gridstand_lk200_shop) as blabla using unique pk_product using srid=2056"
    DATA "the_geom from (SELECT * FROM datenstand.view_gridstand_lk200_shop) as blabla using unique pk_product using srid=2056"
    DATA "the_geom from (SELECT * FROM datenstand.view_gridstand_lk200_shop) as blabla using unique pk_product using srid=2056"
    DATA "the_geom from (SELECT pk_product, s_title_%lang% as label, the_geom FROM datenstand.view_gridstand_lk200_shop) as blabla using unique pk_product using srid=2056"
    DATA "the_geom from (SELECT * FROM datenstand.view_gridstand_lk500) as blabla using unique pk_product using srid=2056"
    DATA "the_geom from (SELECT * FROM datenstand.view_gridstand_lk500) as blabla using unique pk_product using srid=2056"
    DATA "the_geom from (SELECT pk_product, s_title_%lang% as label, the_geom FROM datenstand.view_gridstand_lk500) as blabla using unique pk_product using srid=2056"
    DATA "the_geom from (SELECT * FROM datenstand.view_gridstand_lk500) as blabla using unique pk_product using srid=2056"
    DATA "the_geom from  (SELECT *  from geodaesie.lotabweichungen where st_astext(the_geom) like '%POINT%') as data using unique id using srid=2056"
    DATA "the_geom from  (SELECT *  from geodaesie.lotabweichungen where st_astext(the_geom) like '%LINE%') as data using unique id using srid=2056"
    DATA "the_geom from (
    DATA "the_geom from (
    DATA "the_geom from (
    DATA "the_geom FROM (SELECT bgdi_id, the_geom FROM public.grid_pk100_restblatt) AS data using UNIQUE bgdi_id using srid=2056"
    DATA "the_geom from (SELECT gid, kbnum, lk_name, lk_name as map_label, release, the_geom FROM datenstand.view_gridstand_datenhaltung_pk100_tilecache) as blabla using unique gid using srid=2056"
    DATA "the_geom from (SELECT gid, kbnum, lk_name, release, kbnum as map_label, the_geom FROM datenstand.view_gridstand_datenhaltung_pk100_tilecache) as blabla using unique gid using srid=2056"
    DATA "the_geom from (SELECT gid, release, releasekey, kbnum, lk_name, the_geom FROM datenstand.view_gridstand_datenhaltung_pk100_tilecache) as blabla using unique gid using srid=2056"
    DATA "the_geom from (SELECT gid, kbnum, lk_name, release, lk_name as map_label, the_geom FROM datenstand.view_gridstand_datenhaltung_pk200_tilecache) as blabla using unique gid using srid=2056"
    DATA "the_geom from (SELECT gid, kbnum, lk_name, release, kbnum as map_label, the_geom FROM datenstand.view_gridstand_datenhaltung_pk200_tilecache) as blabla using unique gid using srid=2056"
    DATA "the_geom from (SELECT gid, release, releasekey, kbnum, lk_name, the_geom FROM datenstand.view_gridstand_datenhaltung_pk200_tilecache) as blabla using unique gid using srid=2056"
    DATA "the_geom FROM (SELECT bgdi_id, the_geom FROM public.grid_pk25_restblatt) AS data using UNIQUE bgdi_id using srid=2056"
    DATA "the_geom from (SELECT gid, kbnum, lk_name, lk_name as map_label, release, the_geom FROM datenstand.view_gridstand_datenhaltung_pk25_tilecache) as blabla using unique gid using srid=2056"
    DATA "the_geom from (SELECT gid, kbnum, lk_name, release, kbnum as map_label, the_geom FROM datenstand.view_gridstand_datenhaltung_pk25_tilecache) as blabla using unique gid using srid=2056"
    DATA "the_geom from (SELECT gid, release, releasekey, kbnum, lk_name, the_geom FROM datenstand.view_gridstand_datenhaltung_pk25_tilecache) as blabla using unique gid using srid=2056"
    DATA "the_geom FROM (SELECT bgdi_id, the_geom FROM public.grid_pk50_restblatt) AS data using UNIQUE bgdi_id using srid=2056"
    DATA "the_geom from (SELECT gid, kbnum, lk_name, release, lk_name as map_label, the_geom FROM datenstand.view_gridstand_datenhaltung_pk50_tilecache) as blabla using unique gid using srid=2056"
    DATA "the_geom from (SELECT gid, kbnum, lk_name, release, kbnum as map_label, the_geom FROM datenstand.view_gridstand_datenhaltung_pk50_tilecache) as blabla using unique gid using srid=2056"
    DATA "the_geom from (SELECT gid, release, releasekey, kbnum, lk_name, the_geom FROM datenstand.view_gridstand_datenhaltung_pk50_tilecache) as blabla using unique gid using srid=2056"
    DATA "the_geom FROM (SELECT id, the_geom, title as attribut_c, full_number as attribut_d, concat_ws('', 'http://www.schweizmobil.ch/%lang%/schneeschuhwandern/route', route_number) as link from astra.chmobil_schneeschuhwanderland) AS foo USING UNIQUE id USING srid=2056"
    DATA "the_geom from (SELECT * FROM datenstand.view_gridstand_sfk300) as blabla using unique pk_product using srid=2056"
    DATA "the_geom from (SELECT * FROM datenstand.view_gridstand_sfk300) as blabla using unique pk_product using srid=2056"
    DATA "the_geom from (SELECT pk_product, s_title_%lang% as label, the_geom FROM datenstand.view_gridstand_sfk300) as blabla using unique pk_product using srid=2056"
    DATA "the_geom from (SELECT * FROM datenstand.view_gridstand_stk200) as blabla using unique pk_product using srid=2056"
    DATA "the_geom from (SELECT * FROM datenstand.view_gridstand_stk200) as blabla using unique pk_product using srid=2056"
    DATA "the_geom from (SELECT * FROM datenstand.view_gridstand_stk200) as blabla using unique pk_product using srid=2056"
    DATA "the_geom from (SELECT pk_product, s_title_%lang% as label, the_geom FROM datenstand.view_gridstand_stk200) as blabla using unique pk_product using srid=2056"
    DATA "the_geom from (SELECT * from tlm.swissboundaries_bezirke) as blabla using unique gid using srid=2056"
    DATA "the_geom from (SELECT id, objektart, objektart_lookup, jahr, typ, typ_desc, the_geom FROM tlm.swissboundaries_hoheitsgrenze_hist_wms) AS data using unique id using srid=2056"
    DATA "the_geom from (SELECT bgdi_id as id, objektart, objektart_lookup, typ, typ_desc, the_geom FROM tlm.swissboundaries_hoheitsgrenze_current_wms where objektart_lookup='Kantonsgrenze') as blabla using unique id using srid=2056"
    DATA "the_geom from (SELECT bgdi_id as id, objektart, objektart_lookup, typ, typ_desc, the_geom FROM tlm.swissboundaries_hoheitsgrenze_current_wms where objektart_lookup='Landesgrenze') as blabla USING UNIQUE id USING srid=2056"
    DATA "the_geom from (SELECT * FROM datenstand.swissimage_hist_mapserver_perimeter_%time%) as nofx using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * FROM datenstand.swissimage_hist_chsdi WHERE bgdi_flugjahr = %time%) as nofx using unique bgdi_id using srid=2056"
    DATA "the_geom FROM (SELECT bgdi_id,name,status,objektklasse,objektart,the_geom FROM %zoomlevel% where bgdi_type = 'poly' order by CASE WHEN objektart = 'Gletscher' THEN 1::integer WHEN objektklasse in ('TLM_GEBIETSNAME','TLM_GELAENDENAME','TLM_FLURNAME') THEN 2::integer WHEN objektklasse in ('TLM_STEHENDES_GEWAESSER','TLM_FLIESSGEWAESSER') THEN 3::integer ELSE 0::integer END desc, st_area(the_geom) desc ) as data USING UNIQUE bgdi_id USING srid=2056"
    DATA "the_geom FROM (SELECT bgdi_id,name,status,objektklasse,the_geom FROM %zoomlevel% where bgdi_type = 'line' ) as data USING UNIQUE bgdi_id USING srid=2056"
    DATA "the_geom FROM (SELECT bgdi_id,name,status,objektklasse,objektart,the_geom FROM %zoomlevel% where bgdi_type = 'point' ) as data USING UNIQUE bgdi_id USING srid=2056"
    DATA "the_geom from (SELECT tilekey, tilekey as map_label, temporalkey, the_geom FROM datenstand.view_gridstand_swisssurface3d) as blabla using unique tilekey using srid=2056"
    DATA "the_geom from (SELECT tilekey, tilekey as map_label, fly_y_max, fly_y_min, the_geom FROM datenstand.view_gridstand_swisssurface3d_raster_metadata) as blabla using unique tilekey using srid=2056"
    DATA "the_geom from (SELECT bgdi_id as id, objectid, objektart, verkehrsmittel, standseilbahn, zahnradbahn, ausser_betrieb, anschlussgleis, kunstbaute, %geometry% as the_geom from tlm.eisenbahn) as blabla using unique objectid using srid=2056"
    DATA "the_geom from (SELECT bgdi_id, objectid, name, gwl_nr, objektart, %geometry% as the_geom from tlm.stehendes_gewaesser) as blabla using unique objectid using srid=2056"
    DATA "the_geom from (SELECT bgdi_id, objectid, objektart, verlauf, name, gwl_nr,  %geometry% as the_geom from tlm.fliessgewaesser order by stufe) as blabla using unique objectid using srid=2056"
    DATA "the_geom from (SELECT * from tlm.strasse_aus_einfahrt) as blabla using unique objectid using srid=2056"
    DATA "the_geom from (SELECT * from tlm.strasse limit 0) as blabla using unique objectid using srid=2056"
    DATA "the_geom from (SELECT %geometry% as the_geom, objektart, verkehrsbedeutung, kunstbaute, objectid, bgdi_id, strassenname, belagsart, eigentuemer, verkehrsbeschraenkung from tlm.strasse %filter% ) as blabla using unique objectid using srid=2056"
    DATA "the_geom from (SELECT * from tlm.strasseninfo) as blabla using unique objectid using srid=2056"
    DATA "the_geom from (SELECT bgdi_id as id, name, ausser_betrieb, objectid, objektart, betriebsbahn, %geometry% as the_geom from tlm.uebrige_bahn) as blabla using unique objectid using srid=2056"
    DATA "the_geom from (SELECT bgdi_id, objektart, %geometry% as the_geom from tlm.wald) as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT %geometry% as the_geom, wanderwege_resolved, objectid from tlm.wanderwege) as blabla using unique objectid using srid=2056"
    DATA "the_geom from (SELECT * from tlm.swisstne_base_poly) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * from tlm.swisstne_base_line) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * from tlm.swisstne_base_point) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT bgdi_id, contour, layer, contour || ' cm' as label_lines, the_geom from geodaesie.bezugsrahmen_hoehe_line %filter%) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT bgdi_id, name, y, x, or_ln02_cm, the_geom from geodaesie.bezugsrahmen_hoehe_pkt) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT identifier, sg_name, vali_date, the_geom_buffer_100 as the_geom from vd.spannungsarme_gebiete ) as blabla using unique identifier using srid=2056"
    DATA "the_geom from (Select identifier, sg_name, vali_date, the_geom from vd.spannungsarme_gebiete ) as blabla using unique identifier using srid=2056"
    DATA "the_geom from (SELECT * from public.treasurehunt order by type_coord) as geometrie using unique bgdi_id using srid=2056"
    DATA "the_geom  from (Select gid, id, bfs_nr, gemeindename, abgabestelle, flaeche_ha, the_geom, kanton, pdf_liste from  vd.amogr_gemeinde order by gid asc) as blabla using unique gid using srid=2056"
    DATA "the_geom_gen10  from (Select gid, id, bfs_nr, gemeindename, abgabestelle, flaeche_ha, the_geom_gen10, kanton, pdf_liste from  vd.amogr_gemeinde order by gid asc) as blabla using unique gid using srid=2056"
    DATA "the_geom_gen250  from (Select gid, id, bfs_nr, gemeindename, abgabestelle, flaeche_ha, the_geom_gen250, kanton, pdf_liste from  vd.amogr_gemeinde order by gid asc) as blabla using unique gid using srid=2056"
    DATA "the_geom_gen50  from (Select gid, id, bfs_nr, gemeindename, abgabestelle, flaeche_ha, the_geom_gen50, kanton, pdf_liste from  vd.amogr_gemeinde order by gid asc) as blabla using unique gid using srid=2056"
    DATA "the_geom  from (Select gid, id, bfs_nr, gemeindename, abgabestelle, flaeche_ha, %geometry% as the_geom, kanton, pdf_liste from  vd.amogr_gemeinde order by gid asc) as blabla using unique gid using srid=2056"
    DATA "the_geom from (Select gid,ortsteil_grundbuch,grundbuchfuehrung_d,grundbuchfuehrung_f,grundbuchfuehrung_i,grundbuchkreis,adresse,telefon,email, the_geom from vd.amogr_grundbuch order by gid asc ) as blabla using unique gid using srid=2056"
    DATA "the_geom_gen10 from (Select gid,ortsteil_grundbuch,grundbuchfuehrung_d,grundbuchfuehrung_f,grundbuchfuehrung_i,grundbuchkreis,adresse,telefon,email, the_geom_gen10 from vd.amogr_grundbuch order by gid asc ) as blabla using unique gid using srid=2056"
    DATA "the_geom_gen250 from (Select gid,ortsteil_grundbuch,grundbuchfuehrung_d,grundbuchfuehrung_f,grundbuchfuehrung_i,grundbuchkreis,adresse,telefon,email, the_geom_gen250 from vd.amogr_grundbuch order by gid asc ) as blabla using unique gid using srid=2056"
    DATA "the_geom_gen50 from (Select gid,ortsteil_grundbuch,grundbuchfuehrung_d,grundbuchfuehrung_f,grundbuchfuehrung_i,grundbuchkreis,adresse,telefon,email, the_geom_gen50 from vd.amogr_grundbuch order by gid asc ) as blabla using unique gid using srid=2056"
    DATA "the_geom from (Select gid,ortsteil_grundbuch,grundbuchfuehrung_d,grundbuchfuehrung_f,grundbuchfuehrung_i,grundbuchkreis,adresse,telefon,email, %geometry% as the_geom from vd.amogr_grundbuch order by gid asc ) as blabla using unique gid using srid=2056"
    DATA "the_geom from ( select gid, name, firmenname, adresse, email, telefon, the_geom from  vd.amogr_nfgeom order by gid asc ) as blabla using unique gid using srid=2056"
    DATA "the_geom_gen10 from ( select gid, name, firmenname, adresse, email, telefon, the_geom_gen10 from  vd.amogr_nfgeom order by gid asc ) as blabla using unique gid using srid=2056"
    DATA "the_geom_gen250 from ( select gid, name, firmenname, adresse, email, telefon, the_geom_gen250 from  vd.amogr_nfgeom order by gid asc ) as blabla using unique gid using srid=2056"
    DATA "the_geom_gen50 from ( select gid, name, firmenname, adresse, email, telefon, the_geom_gen50 from  vd.amogr_nfgeom order by gid asc ) as blabla using unique gid using srid=2056"
    DATA "the_geom from ( select gid, name, firmenname, adresse, email, telefon, %geometry% as the_geom from  vd.amogr_nfgeom order by gid asc ) as blabla using unique gid using srid=2056"
    DATA "the_geom from (SELECT bgdi_id, canton, the_geom, description, year from  vd.amopnf_pnf)as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom from (Select gid,the_geom,quality,frame from  vd.amogr_standav order by gid asc) as blabla using unique gid using srid=2056"
    DATA "the_geom_gen10 from (Select gid,the_geom_gen10,quality,frame from  vd.amogr_standav order by gid asc) as blabla using unique gid using srid=2056"
    DATA "the_geom_gen250 from (Select gid,the_geom_gen250,quality,frame from  vd.amogr_standav order by gid asc) as blabla using unique gid using srid=2056"
    DATA "the_geom_gen50 from (Select gid,the_geom_gen50,quality,frame from  vd.amogr_standav order by gid asc) as blabla using unique gid using srid=2056"
    DATA "the_geom from (Select gid, %geometry% as the_geom,quality,frame from  vd.amogr_standav order by gid asc) as blabla using unique gid using srid=2056"
    DATA "the_geom from (SELECT gid, the_geom, plz, zusziff, langtext, rgbcode, (plz ||'&'|| langtext) as tot from  vd.gabmo_plz)as blabla using unique gid using srid=2056"
    DATA "the_geom from vd.spannungsarme_gebiete using unique identifier using srid=2056"
    DATA "the_geom from ( SELECT gid as id, id as fid, gemeindename, bgdi_id, bgdi_status, bfs_nr, bfs_name, kanton, oereb_status_de, oereb_status_fr, oereb_status_it, oereb_status_rm, oereb_status_en, firmenname, adresszeile, plz, ort, telefon, email, url_oereb, %geometry% as the_geom FROM vd.view_oereb_nfgeom ORDER BY bgdi_status desc) as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom from ( SELECT gid as id, id as fid, oereb_extract_pdf, oereb_extract_url, number_ as number, realestate_type, gemeindename, gid, bgdi_status, bfs_nr, bfs_name, kanton, oereb_status_de, oereb_status_fr, oereb_status_it, oereb_status_rm, oereb_status_en, firmenname, adresszeile, plz, ort, telefon, email, url_oereb, egris_egrid, oereb_webservice, the_geom FROM vd.view_stand_oereb_parcel WHERE realestate_type LIKE 'Liegenschaft') as blabla using unique gid using srid=2056"
    DATA "the_geom from ( SELECT gid as id, id as fid, oereb_extract_pdf, oereb_extract_url, number_ as number, realestate_type, gemeindename, gid, bgdi_status, bfs_nr, bfs_name, kanton, oereb_status_de, oereb_status_fr, oereb_status_it, oereb_status_rm, oereb_status_en, firmenname, adresszeile, plz, ort, telefon, email, url_oereb, egris_egrid, oereb_webservice, the_geom FROM vd.view_stand_oereb_parcel WHERE realestate_type NOT LIKE 'Liegenschaft') as blabla using unique gid using srid=2056"
    DATA "the_geom from vec200_protectedarea USING UNIQUE gtdboid USING srid=2056"
    DATA "the_geom from vec200_building USING UNIQUE gtdboid USING srid=2056"
    DATA "the_geom from vec200_lake USING UNIQUE gtdboid USING srid=2056"
    DATA "the_geom from vec200_dam using unique gtdboid using srid=2056"
    DATA "the_geom from vec200_flowingwater USING UNIQUE gtdboid USING srid=2056"
    DATA "the_geom FROM vec200_hydroinfo USING UNIQUE gtdboid USING srid=2056"
    DATA "the_geom from vec200_stagnantwater using unique gtdboid using srid=2056"
    DATA "the_geom FROM (SELECT %geometry% as the_geom, objval, gtdboid, objname1, ppl, ppi FROM vec200_landcover) as data USING UNIQUE gtdboid USING srid=2056"
    DATA "the_geom from vec200_geodpoint using unique gtdboid using srid=2056"
    DATA "the_geom  from vec200_builtupp USING UNIQUE bgdi_id USING srid=2056"
    DATA "the_geom FROM vec200_physl USING UNIQUE objectid USING srid=2056"
    DATA "the_geom from vec200_poi using unique gtdboid using srid=2056"
    DATA "the_geom FROM vec200_supply USING UNIQUE gtdboid USING srid=2056"
    DATA "the_geom FROM vec200_namedlocation USING UNIQUE gtdboid USING srid=2056"
    DATA "the_geom from vec200_airport using unique gtdboid using srid=2056"
    DATA "the_geom from vec200_customsoffice using unique gtdboid using srid=2056"
    DATA "the_geom from vec200_interchange using unique gtdboid using srid=2056"
    DATA "the_geom from vec200_railway using unique gtdboid using srid=2056"
    DATA "the_geom from vec200_ramp using unique gtdboid using srid=2056"
    DATA "the_geom from (SELECT %geometry% as the_geom, gtdboid, objval, construct from vec200_road) as data using unique gtdboid using srid=2056"
    DATA "the_geom from vec200_runway using unique gtdboid using srid=2056"
    DATA "the_geom from vec200_ship using unique gtdboid using srid=2056"
    DATA "the_geom from vec200_ship using unique gtdboid using srid=2056"
    DATA "the_geom from vec200_terminal using unique gtdboid using srid=2056"
    DATA "the_geom from vec200_trafficinfo using unique gtdboid using srid=2056"
    DATA "the_geom FROM v25_eob_25_l USING UNIQUE objectid USING SRID=2056"
    DATA "the_geom FROM v25_eob_25_p USING UNIQUE objectid USING SRID=2056"
    DATA "the_geom FROM v25_geb_25_a USING UNIQUE objectid USING SRID=2056"
    DATA "the_geom from (SELECT * FROM v25_gewaessernetz_ref) AS bla USING UNIQUE bgdi_id USING SRID=2056"
    DATA "the_geom FROM v25_heb_25_l USING UNIQUE objectid USING SRID=2056"
    DATA "the_geom FROM v25_heb_25_p USING UNIQUE objectid USING SRID=2056"
    DATA "the_geom FROM (select objectid, objectval, area, perimeter, %geometry% as the_geom from public.v25_pri25_a) as data USING UNIQUE objectid USING srid=2056"
    DATA "the_geom from (SELECT the_geom,gid from v25_pri25_a_seeflaechen) as blabla using unique gid using srid=2056"
    DATA "the_geom from (SELECT id, %geometry% as the_geom,name,type,e_lv03,n_lv03,e_lv95,n_lv95,de,dn,fs FROM geodaesie.verschiebungsvektoren_tsp1) as blabla using unique id using srid=2056"
    DATA "the_geom from (SELECT id, %geometry% as the_geom,name,type,e_lv03,n_lv03,e_lv95,n_lv95,de,dn,fs FROM geodaesie.verschiebungsvektoren_tsp2) as blabla using unique id using srid=2056"
    DATA "the_geom from (SELECT * FROM datenstand.view_gridstand_lkwander25zus_shop) as blabla using unique pk_product using srid=2056"
    DATA "the_geom from (SELECT * FROM datenstand.view_gridstand_lkwander25zus_shop) as blabla using unique pk_product using srid=2056"
    DATA "the_geom from (SELECT * FROM datenstand.view_gridstand_lkwander25zus_shop) as blabla using unique pk_product using srid=2056"
    DATA "the_geom from (SELECT * FROM datenstand.view_gridstand_lkwander25zus_shop) as blabla using unique pk_product using srid=2056"
    DATA "the_geom from (SELECT * FROM datenstand.view_gridstand_lkwander25zus_shop) as blabla using unique pk_product using srid=2056"
    DATA "the_geom from (SELECT * FROM datenstand.view_gridstand_wkt33_shop) as blabla using unique pk_product using srid=2056"
    DATA "the_geom from (SELECT * FROM datenstand.view_gridstand_wkt33_shop) as blabla using unique pk_product using srid=2056"
    DATA "the_geom from (SELECT * FROM datenstand.view_gridstand_wkt33_shop) as blabla using unique pk_product using srid=2056"
    DATA "the_geom from (SELECT * FROM datenstand.view_gridstand_wkt33_shop) as blabla using unique pk_product using srid=2056"
    DATA "the_geom FROM (SELECT bgdi_id, bgdi_id as id, inventar_nr, kanton, standort, bezeichnung, kategorie, thema, zeitraum, form, baujahr, link_zum_objekt, link_zum_gesamtinventar, kat_nr, the_geom FROM militaer.armee_kriegsdenkmaeler) as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT the_geom, name, email as mail, abkuerzung, url, bgdi_id from militaer.armeelogistikcenter) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT *, produkt_de as produkt, hinweis_de as hinweis, x as x_koord, y as y_koord from militaer.bundestankstellen_bebeco) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT *, standorttyp_de as standorttyp, untersuchungsmassnahmen_de as untersuchungsmassnahmen FROM militaer.kataster_belasteter_standorte_militaer WHERE ST_Geometrytype(the_geom) = 'ST_MultiPolygon') as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT * FROM militaer.kataster_belasteter_standorte_militaer WHERE ST_Geometrytype(the_geom) = 'ST_Point') as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom from (select bgdi_id, the_geom, stabil_id, bgdi_class, class_weight, text_aussage_%lang% as text_aussage, text_thema_%lang% as text_thema, text_rechtsstatus_%lang% as text_rechtsstatus, publiziertab, publiziertbis, text_amt_%lang% as text_amt FROM militaer.kataster_belasteter_standorte_militaer_v2_0oereb ORDER BY class_weight DESC,st_area(the_geom) DESC) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (select  bgdi_id, the_geom, stabil_id, bgdi_class, class_weight, text_aussage_%lang% as text_aussage, text_thema_%lang% as text_thema, text_rechtsstatus_%lang% as text_rechtsstatus, publiziertab, publiziertbis, text_amt_%lang% as text_amt FROM militaer.kataster_belasteter_standorte_militaer_v2_0oereb ORDER BY class_weight DESC,st_area(the_geom) DESC) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (select bgdi_id, the_geom, stabil_id, bgdi_class, class_weight, text_aussage_%lang% as text_aussage, text_thema_%lang% as text_thema, text_rechtsstatus_%lang% as text_rechtsstatus, publiziertab, publiziertbis, text_amt_%lang% as text_amt FROM militaer.kataster_belasteter_standorte_militaer_v2_0oereb WHERE geom_type LIKE '%point%' ORDER BY class_weight DESC) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT *, kantone as kanton from militaer.abschnittsregionen_armeelogistikzentren) as data using unique objectid using srid=2056"
    DATA "the_geom from (SELECT the_geom, bgdi_id, netztyp FROM militaer.panzerverschiebungsrouten_strecken) as data using unique bgdi_id using srid=2056"
    DATA "the_geom FROM (SELECT the_geom, bgdi_id, typ, tonnage FROM militaer.panzerverschiebungsrouten_kunstbauten) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT bgdi_id, name, the_geom  from militaer.patrouilledesglaciers) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT bgdi_id, name, the_geom  from militaer.patrouilledesglaciers) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT the_geom, bgdi_id, name, url from militaer.retablierungsstellen) as data using unique bgdi_id using srid=2056"
    DATA "the_geom from  (SELECT * from militaer.sim_pl_r_anhoerung where st_geometrytype(the_geom) = 'ST_MultiPolygon' ) as data using unique stabil_id using srid=2056"
    DATA "the_geom from  (SELECT * from militaer.sim_pl_anhoerung where st_geometrytype(the_geom) = 'ST_MultiPolygon' ) as data using unique stabil_id using srid=2056"
    DATA "the_geom from  (SELECT *  from militaer.sim_pl_anhoerung where st_geometrytype(the_geom) = 'ST_MultiPoint') as data using unique stabil_id using srid=2056"
    DATA "the_geom from  (SELECT *  from militaer.sim_fac_anhoerung) as data using unique stabil_id using srid=2056"
    DATA "the_geom from  (SELECT * from militaer.sim_pl_r_kraft where st_geometrytype(the_geom) = 'ST_MultiPolygon' ) as data using unique stabil_id using srid=2056"
    DATA "the_geom from  (SELECT * from militaer.sim_pl_kraft where st_geometrytype(the_geom) = 'ST_MultiPolygon' ) as data using unique stabil_id using srid=2056"
    DATA "the_geom from  (SELECT * from militaer.sim_pl_kraft) as data using unique stabil_id using srid=2056"
    DATA "the_geom from  (SELECT *  from militaer.sim_fac_kraft) as data using unique stabil_id using srid=2056"
    DATA "the_geom FROM (SELECT * FROM public.schiessanzeigen) as blabla using unique belplan_id using srid=2056"
    DATA "the_geom from (SELECT bgdi_id, terreg_nr, name, the_geom from territorialregionen) as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom from (SELECT bgdi_id, terreg_nr, name, 'XX'::text as xx, ST_BOUNDARY(the_geom) AS the_geom from territorialregionen) as blabla using unique bgdi_id using srid=2056"
    DATA "the_geom FROM (SELECT * from wascha.projektil) as blabla USING UNIQUE bgdi_id USING srid=2056"
